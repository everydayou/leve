================================================================
NUTRITION GOAL TRACKER — PROJECT CONTEXT
================================================================
Owner: Marco (marcosilva86@gmail.com)
Last updated: 2026-07-04 — Round 160 (Every numeric-keyboard input app-wide now uses the Keyboard-playground row-4 pattern: decimal/numeric keypad + custom "Done" bar, via a shared useNumericDoneBar hook)
Full change history: see CHANGELOG.txt (rounds 1–106).

HOW TO READ THIS DOC:
- This file = current state only. Read it at the start of every chat.
- CHANGELOG.txt = full per-round history. Open it when you need to know
  WHY something was decided or what changed in a specific round.
- Before touching any UI: read section 0B (Design-System Contract) first.
- If two statements conflict: the higher round number wins.

----------------------------------------------------------------
0. QUICK STATUS
----------------------------------------------------------------
- App is RUNNING on Marco's iPhone (native Capacitor build, free Apple ID).
  On-device install: DONE as of 2026-06-04 (runbook: INSTALL-iOS.md).
- THREE PREVIEW SURFACES, each with its OWN separate data store:
  (1) iPhone native app (Capacitor) — Marco's REAL daily-use data; IndexedDB
      in the app container; persists across rebuilds/re-signing.
  (2) Browser dev server (`npm run dev` -> localhost) — real app with
      hot-reload; IndexedDB local to that browser; SEPARATE from the phone.
  (3) Double-click demo HTML (file://) — in-memory, resets on refresh.
  Data does NOT sync between surfaces.
- V1 FEATURE STATUS: feature-complete and running on phone.
- VERIFY-ON-MAC REMINDER: after Cowork changes, run on Mac:
  `npm install` → `npm test` → `npm run build` → `npm run ios:sync`.

----------------------------------------------------------------
0B. DESIGN-SYSTEM CONTRACT — FIXED RULES (read BEFORE editing any UI)
----------------------------------------------------------------
Non-negotiable standing rules. Any chat touching src/ui MUST follow them.

1. COMPONENTS FIRST. Build UI from kit atoms in src/ui/kit: Button/IconButton,
   Sheet, LabeledInput, NumberField, Card/Surface, StatTile, ProgressBar/
   GaugeArc, Badge, SegmentedControl, FilterPills, Icon, etc. NEVER hand-roll
   a button/input/sheet/stat inline when a kit atom exists.
2. TOKENS ONLY. Colour, radius, shadow and type from src/index.css semantic
   tokens. NEVER raw Tailwind text sizes (text-xs/sm/base/lg/xl) — use
   text-caption/footnote/subhead/body/callout/headline/title/display/hero.
   NEVER hardcode hex/rgb — use colour tokens.
3. ICONS, NOT GLYPHS. Use <Icon> for all affordances. Never type ›, ‹ or ×.
4. ONE SOURCE OF TRUTH. Change look via token or atom, never per-screen CSS.
5. ENFORCEMENT. eslint.config.js BLOCKS raw text sizes and hex/rgb in src/ui.
   Run `npm run lint` before shipping. Do NOT disable guardrails.
6. ACCESSIBILITY FIRST. Focus outlines, ARIA roles, prefersReducedMotion(),
   WCAG AA contrast, 44×44pt touch targets, labeled form inputs.

Text scale: caption(12) / footnote(13) / subhead(15) / callout(16) /
headline(18) / title(22) / display(30) / hero(48).
WHERE THINGS LIVE: tokens = src/index.css (@theme); atoms = src/ui/kit/;
live reference = /styleguide route.

----------------------------------------------------------------
1. WHAT WE'RE BUILDING
----------------------------------------------------------------
A personal nutrition + weight-management app for Marco's own use.
NOT a calorie tracker. Core idea: GOAL MANAGEMENT — tracking exists only
to answer "Am I on track to hit my goal?"
Mental model: Goal → Weekly progress → Daily progress → Food/Activity inputs.

----------------------------------------------------------------
2. PLATFORM & STACK
----------------------------------------------------------------
React + Capacitor (web code + native iOS shell). NOT native Swift.
Stack: Vite 6, React 19, TypeScript, Tailwind CSS v4, Dexie (IndexedDB),
Vitest. Capacitor v8. appId: com.marco.nutritiongoal, webDir: dist.
Install path: Xcode + free Apple ID ($0). Re-sign every ~7 days.

----------------------------------------------------------------
3. AGREED V1 SCOPE
----------------------------------------------------------------
- Goal types: lose_by_date and gain_by_date.
- TODAY: frequent foods, Add-entry sheet, calories + protein, deficit,
  swipeable day nav.
- GOAL: weight trend vs. target line, 9 lifecycle states.
- PANTRY: create/edit food items.
- Manual meal entry, manual activity calories, weight logging.
- BMR: single user-entered number in V1.
- Account: weight history, profile, BMR, export (JSON).
DEFER (V1.5+): BMR auto-calibration, HealthKit/Withings, History tab,
per-piece measurement types.

----------------------------------------------------------------
4. NAVIGATION / IA
----------------------------------------------------------------
- Bottom tab bar: Today / Goal / Pantry / Account.
- Center "+" FAB speed dial: Scan / Food / Activity / Weight.
  Scan item only shown when VITE_FOOD_SCAN_API_URL is set (SCAN_ENABLED gate).
- Add-entry = bottom sheet (never full-screen nav).
- Goal Setup = full-screen flow. ?skip-type=true skips plan-type step.
- Day nav on Today: chevrons + swipe. Future dates disabled.

----------------------------------------------------------------
4B. V1 DATA MODEL
----------------------------------------------------------------
Goal status: active / completed / abandoned.
GoalScreen renders 9 states (all implemented r55):
  (1) no-goal  (2) active-normal  (3) final-day·no-weight
  (4) final-day·weight-in(not hit)  (5) final-day·goal-hit
  (6) early-complete  (7) overdue  (8) completed  (9) abandoned/ended
GoalOutcomeView: createPortal → document.body. Slide-up on mount.
GoalSettingsSheet (⋯): Mark complete / Edit plan / End goal / Past goals.
User model: weightCadence ('daily'|'weekly'), weeklyWeightDay.

----------------------------------------------------------------
5. METRICS / MATH
----------------------------------------------------------------
Total Burn = BMR + Active Calories.
Daily Deficit = Total Burn − Calories Consumed.
~7700 kcal per kg for goal pace math.

----------------------------------------------------------------
7. CURRENT STATUS (as of round 122, 2026-07-02)
----------------------------------------------------------------
All V1 features shipped and running on device. Round 122 introduced a unified
FoodItemForm component (single source of truth for all 4 food-item edit
surfaces), fully wired "Save to pantry" flow from basket-edit (including DB
persistence), collage photo cap raised to 4, and multiple basket polish fixes.
- Round 122 (2026-07-02): FoodItemForm.tsx (new shared component); PantryScreen
  + EditOverlay + ManualOverlay replaced with thin wrappers; "Save to pantry"
  fully wired (pantryItemId stamped on basket item, DB entry updated);
  photo cap 3→4; frequentItems in LogEntryContent; delete-item photo cleanup;
  fast-log bypass on search; collage position tweaks; nutri-scan "Change"
  button removed.
- Round 121 (2026-06-30): FAB de-animated; BasketCard always-visible edit+trash
  buttons; EditIcon custom SVG; stepper 40px container; ImageHero collage
  layouts for 2/3/4 photos with Figma-corrected coordinates.
- Round 119 (2026-06-30): mealData.photos[] type added; FoodForm saves all
  scan photos + timeMealName() fallback; LogEntryContent: init from photos[],
  editable meal name, onCorrect scoped to item.sourceId.
- .env.local: gitignored, must be recreated if folder is deleted.
  VITE_FOOD_SCAN_API_URL=https://food-scan-api-delta.vercel.app
- tsc: clean. lint: clean.

----------------------------------------------------------------
8. ARCHITECTURE NOTES
----------------------------------------------------------------
Layer model (strict dependency direction):
  src/domain/   Pure TS — types.ts, calc.ts, goal.ts, bmr.ts.
  src/data/     Dexie + memory repos. repositories.ts = interfaces.
  src/state/    repos.ts (swap point) + useLive() + useDay().
  src/ui/       AppShell + screens + components + kit/.
  src/index.css Design token layer (Tailwind v4 @theme).

KEY PATTERNS:
- createPortal: GoalOutcomeView renders into document.body to escape
  AppShell overflow-hidden / z-index on WKWebView.
- slide-up/down: .slide-up-in / .slide-down-out in index.css.
  Exit: setExiting(true) → setTimeout(cb, 320) → navigate.
- Sheet-on-Sheet: destructive confirms render a second Sheet alongside
  the parent in a Fragment (both conditionally mounted).
- SCAN_ENABLED = !!(import.meta.env.VITE_FOOD_SCAN_API_URL) — used in
  AddEntrySheet.tsx header icon and FloatingTabBar.tsx FAB action list.

ENV NOTE: Cowork Linux sandbox can't run rollup native binary. After
changes, on the Mac: `npm install` → `npm test` → `npm run build` →
`npm run ios:sync`.

----------------------------------------------------------------
KNOWN GAPS
----------------------------------------------------------------
HIGH:
- SHEET stickyHeader SLOT: verify SegmentedControl renders in the correct
  pinned-above-scroll position on device.
- SHEET SCROLL-SHADOW BUG (open, round 152/153): main Sheet header's shadow
  disappears under scrolled content; a round-152 fix attempt made it worse
  and was reverted in round 153. Needs an actual device screenshot/repro
  rather than another source-only guess.

MED:
- NO CONFIRM on destructive deletes of food entries, pantry items, weight
  entries. (Goal actions confirm via Sheet; entry deletes are immediate.)
- ACCOUNT IA: layout still growing — grouping pass worthwhile when more
  prefs land.
- .env.local is gitignored and must be recreated manually if the project
  folder is deleted. Contains VITE_FOOD_SCAN_API_URL (Vercel scan API URL).

LOW:
- GoalOutcomeView: no haptic on appearance.
- App.tsx still has /past-goals and /past-goals/:id stub routes (now unused;
  PastGoalsPortal is portal-based). Safe to remove eventually.
- ACTIVITY TEACHING MOMENT (§9 first-open spec): deferred.
- GoalForkScreen (/goal-fork): built but not device-reviewed.
- ProfileSetupSheet height select always shows cm. Low priority.
- Estimated BMR (weight-only) assumes pop-avg 170 cm / 35 yr / neutral sex.

DONE (r122):
- ✓ FoodItemForm.tsx: unified shared form for all 4 food-item surfaces.
- ✓ PantryScreen: thin wrapper around FoodItemFormContent; delete icon updated.
- ✓ EditOverlay + ManualOverlay: replaced with FoodItemFormContent wrappers.
- ✓ "Save to pantry" from basket-edit: pantryItemId stamped on basket item;
    DB entry updated (foodItemId + isManual:false); photo forwarded correctly.
- ✓ addManualItem (both contexts): pantry id shared with basket item.
- ✓ "Saved to pantry" shows checked+disabled when item already linked.
- ✓ Photo cap 3 → 4 everywhere; ImageHero cfg3/cfg4 position tweaks.
- ✓ LogEntryContent frequentItems via useLive(frequentItemIds).
- ✓ Delete-item photo cleanup in LogEntryContent.
- ✓ addPantryItem photo wiring in LogEntryContent.
- ✓ Fast-log bypass when fromSearch=true (FoodPicker → FoodForm).
- ✓ Nutri-scan "Change" button removed from BasketCard.
- ✓ MeasurementTypeSelector in EditOverlay + ManualOverlay.
- ✓ PantryScreen delete icon updated to new filled design.

DONE (r121):
- ✓ FAB fully de-animated (hapticLight + onFabMorphComplete directly).
- ✓ BasketCard: always-visible edit+trash circular buttons (no swipe/editMode).
- ✓ EditIcon: custom SVG from design spec (16×16 fill path).
- ✓ All buttons + stepper container: h-10 (40px), icons 20px, stepper gap-0.
- ✓ ImageHero: 2/3/4-photo collage with Figma-corrected bounding-box coords.

DONE (r119):
- ✓ mealData.photos?: string[] added to FoodEntry type.
- ✓ FoodForm logBasket: saves photos[]; fallback 'Meal'→timeMealName().
- ✓ LogEntryContent: localPhotos init chain, editable meal name, onCorrect
    scoped to item.sourceId, correctingIdx state.

DONE (r115):
- ✓ LogEntrySheet: unified basket-style entry editor replacing EditFoodSheet
    + MealEditSheet. All 3 cases (pantry/manual/meal) shown as cards.
- ✓ BasketCard: "Fix/Change" button + onCorrect prop for re-describing scanned items.
- ✓ ManualOverlay: inline CTA Button, 256×256 photo style, keyboard scroll fix.
- ✓ Macro rounding: max 1 decimal, trailing zero trimmed (parseFloat toFixed(1)).

DONE (r114):
- ✓ Describe overlay redesign: 22-char name rules, non-food rejection, improved prompt.
- ✓ Vercel describe-food endpoint: model changed haiku → claude-sonnet-4-6.
- ✓ Sheet scroll: removed overflowX hidden from both scroll areas (iOS scroll chaining fix).
- ✓ OverlayNav centering: back button wrapped in w-10 shrink-0 span.
- ✓ AddAnotherSection: single-container pattern with bare FoodPicker prop.

DONE (r111):
- ✓ Overlay panels: swipe right → go back (touch handler on OverlayLayer).
- ✓ OverlayNav: shared sticky title bar + scroll shadow for all overlays.
- ✓ Single food photo: 256×256 square (was 4:3 wide).
- ✓ Duplicate pantry item in basket → increments qty instead of new entry.
- ✓ Meal header hidden when basket is empty.
- ✓ Frequent-foods quick-log grid removed from Diary.
- ✓ Log button always active (removed disabled state).
- ✓ Recent items "+" button: solid accent bg, black icon.
- ✓ Basket cards: removed thumbnail + protein, stepper always visible,
    tap card (normal mode) → edit overlay, edit mode → trash only beside
    card, swipe left reveals trash, returning from edit resets swipe state.
- ✓ Stepper at 0 → trash icon → tap removes item.
- ✓ "Add another item": grey bg, black text, visible in edit mode.
- ✓ Expanded picker: heading row with X left + title centred.
- ✓ Method cards: text-content (not secondary), overflow clipping fixed.
- ✓ ManualOverlay: photo upload added (same style as EditOverlay).
- ✓ EditOverlay: sticky nav via OverlayNav, non-sticky Save + Cancel,
    photo trash icon + "Change photo" white-border button.
- ✓ PantryScreen FoodItemForm: updated to new EditOverlay-style design.

DONE (r108):
- ✓ Add-Entry design spec written (leve-add-entry-design-spec.docx).
- ✓ Interactive HTML prototype built (add-entry-prototype.html).

DONE (r107):
- ✓ MacroDetailSheet horizontal clip without blocking vertical scroll.
- ✓ BreakdownSheet intro text now hides when info panel slides in.
- ✓ GoalSetupScreen Section 2 pre-fills from saved profile on every open.
- ✓ Gauge center number sign removed; sub-labels consistent.
- ✓ Custom filled info icon deployed everywhere.

DONE (r106):
- ✓ KEYBOARD/VIEW CONFLICT: content jumping + black void when keyboard opened
  in sheets. Fixed via @capacitor/keyboard resize:'none' + Capacitor
  keyboardWillShow/Hide events + scrollFocusedAboveKeyboard().

DEFERRED V1.5+: JSON import, HealthKit, BMR auto-calibration, full tablet
layouts, per-piece measurement types, time-of-day food suggestions.

----------------------------------------------------------------
RECENT ROUNDS (3 most recent — older entries in CHANGELOG.txt)
----------------------------------------------------------------

ROUND 160 — NUMERIC-KEYBOARD "DONE" BAR PROMOTED APP-WIDE (2026-07-04)
Files: src/ui/kit/useNumericDoneBar.tsx (new), src/ui/kit/NumberField.tsx,
       src/ui/kit/index.ts, src/ui/components/AddEntrySheet.tsx,
       src/ui/screens/GoalSetupScreen.tsx, src/ui/screens/TodayScreen.tsx,
       src/ui/screens/AccountScreen.tsx
- Extracted the Keyboard-playground row-4 pattern (decimal keypad + a
  "Done" bar that slides in above the keyboard on focus, using
  useKeyboardInset()) into a shared useNumericDoneBar() kit hook, then
  applied it to every numeric-keyboard input in the app, not just the Dev
  playground: NumberField (covers Calories/Protein/Carbs/Fat/Fiber/
  Serving size/Est. weight across FoodItemForm and AddEntrySheet's
  scan-result editor), the Day's-log basket quantity stepper's tap-to-type
  input, GoalSetupScreen's Height/Age fields, the Activity sheet's
  Calories field (both TodayScreen's edit and AddEntrySheet's manual-add
  versions), and AccountScreen's protein-target field. NumberField also
  now carries lang="en-US" (round 159's decimal-separator fix) and gained
  an optional placeholder prop to preserve the two fields that used one.
  GoalSetupScreen/TodayScreen/AccountScreen's raw LabeledInput+inputMode
  usages for these fields were replaced with NumberField outright, so
  there's now exactly one numeric-input implementation for the whole app
  instead of several hand-rolled copies.
- Also this round: Day's-log Activity list-item row's name/kcal weights
  were backwards vs. the Food row convention (name bold, amount regular) —
  swapped to match (src/ui/screens/TodayScreen.tsx).
- tsc: clean. lint: clean. vitest: 48 passed, 8 skipped (unchanged).
  `npm run build` verified clean.

ROUND 159 — KEYBOARD PLAYGROUND ROW 4: FORCE "." SEPARATOR, 24PX DONE SPACING (2026-07-04)
Files: src/ui/components/DevMenu.tsx
- Row 4 (decimal + custom Done bar) confirmed as the one Marco wants. Fixed
  the decimal key showing "," instead of "." (iOS region-dependent, not
  app-language-dependent) via lang="en-US" on that input, plus an onChange
  safety net replacing any "," with ".". Done button's spacing from the
  right edge is now a precise 24px (was 16px all around).
- tsc: clean. lint: clean. vitest: 48 passed, 8 skipped (unchanged).
  `npm run build` verified clean.

ROUND 158 — DEV: KEYBOARD PLAYGROUND (2026-07-04)
Files: src/ui/components/DevMenu.tsx
- New "Keyboards" section in Account > Developers (mirrors "Native
  pickers"/Picker playground exactly, same PickerRow/numRange helpers).
  "Keyboard playground" Sheet with 4 number-input rows: inputMode=numeric,
  inputMode=decimal (current app-wide approach), type=tel, and
  inputMode=decimal + a custom "Done" toolbar sliding in above the keyboard
  on focus (using the same useKeyboardInset() hook Sheet.tsx already uses)
  — addresses Marco's ask for a numeric keyboard with a closing/confirming
  action, since iOS's native number pad has no Return key at all. Doubles
  as a working reference for the long-standing "KEYBOARD TOOLBAR" Known Gap.
- tsc: clean. lint: clean. vitest: 48 passed, 8 skipped (unchanged).
  `npm run build` verified clean.

ROUND 156 — MEAL-SUMMARY-CARD SPACING TWEAK: NAME ROW -> BADGES TO 8PX (2026-07-04)
Files: src/ui/components/PantryMealDetail.tsx, src/ui/components/AddEntrySheet.tsx
- Round 155 set name-row-to-badges to 4px everywhere; Marco wants the
  meal-summary card specifically at 8px instead (Basket/Pantry item cards
  stay at 4px, confirmed "perfect"). MacroSummaryLine's className: mt-1 ->
  mt-2 in both meal-summary card instances only.
- tsc: clean. lint: clean. vitest: 48 passed, 8 skipped (unchanged).
  `npm run build` verified clean.

ROUND 156 — MEAL-SUMMARY-CARD SPACING TWEAK: NAME ROW -> BADGES TO 8PX (2026-07-04)
Files: src/ui/components/PantryMealDetail.tsx, src/ui/components/AddEntrySheet.tsx
- Round 155 set name-row-to-badges to 4px everywhere; Marco wants the
  meal-summary card specifically at 8px instead (Basket/Pantry item cards
  stay at 4px, confirmed "perfect"). MacroSummaryLine's className: mt-1 ->
  mt-2 in both meal-summary card instances only.
- tsc: clean. lint: clean. vitest: 48 passed, 8 skipped (unchanged).
  `npm run build` verified clean.

ROUND 155 — CARD INTERNAL SPACING (4px/16px) + KCAL BADGE HEIGHT MATCH (2026-07-04)
Files: src/ui/components/AddEntrySheet.tsx, src/ui/components/PantryItemCard.tsx,
       src/ui/components/PantryMealDetail.tsx
- BasketCard/PantryItemCard: name+kcal row -> badges tightened to 4px (was
  8px); badges -> stepper/buttons row widened to 16px (was 10px). Same
  ratio applied inside the meal-summary card (both views): 4px name row ->
  badges; Day's-log's "Save to pantry" checkbox below the badges widened to
  16px via inline style (was capped at 12px by the parent's space-y-3 —
  same collapse-proofing pattern as round 154). Pantry's Meal detail has no
  row below its badges, so only the top gap applied.
- Kcal badge (beside the meal name field) given `border border-transparent`
  to match LabeledInput's own 1px border — the one structural difference
  that was making it render very slightly shorter than the name field.
- tsc: clean. lint: clean. vitest: 48 passed, 8 skipped (unchanged).
  `npm run build` verified clean.

ROUND 155 — CARD INTERNAL SPACING (4px/16px) + KCAL BADGE HEIGHT MATCH (2026-07-04)
Files: src/ui/components/AddEntrySheet.tsx, src/ui/components/PantryItemCard.tsx,
       src/ui/components/PantryMealDetail.tsx
- BasketCard/PantryItemCard: name+kcal row -> badges tightened to 4px (was
  8px); badges -> stepper/buttons row widened to 16px (was 10px). Same
  ratio applied inside the meal-summary card (both views): 4px name row ->
  badges; Day's-log's "Save to pantry" checkbox below the badges widened to
  16px via inline style (was capped at 12px by the parent's space-y-3 —
  same collapse-proofing pattern as round 154). Pantry's Meal detail has no
  row below its badges, so only the top gap applied.
- Kcal badge (beside the meal name field) given `border border-transparent`
  to match LabeledInput's own 1px border — the one structural difference
  that was making it render very slightly shorter than the name field.
- tsc: clean. lint: clean. vitest: 48 passed, 8 skipped (unchanged).
  `npm run build` verified clean.

ROUND 154 — MACRO BADGE SIZE, MEAL-CARD BORDER TOKEN, EXACT PIXEL SPACING (2026-07-04)
Files: src/ui/kit/MacroSummaryLine.tsx, src/ui/components/PantryMealDetail.tsx,
       src/ui/components/AddEntrySheet.tsx
- Macro badges: text stepped down to text-footnote (13px, was text-subhead
  15px). Meal summary card border switched to border-border-card-no-shadow
  (no literal "border-hover" token exists; this is the documented token for
  a flat, no-shadow card — the exact category this card is in since round
  153). Kcal badge next to the meal-name field now bg-surface-sunken,
  matching the name input's own background.
- Spacing made exact via inline styles instead of relying on the ambient
  space-y utility, after Marco reported the rendered image-to-card gap was
  only ~4px (not the 16-24px space-y implied) and inconsistent between the
  Day's-log and Pantry views. Summary card: `marginTop: '24px'` inline, same
  in both views. "Food items" heading + its item list share a small wrapper
  so the heading's own 8px bottom margin doesn't collapse against the first
  item card's auto margin-top — guarantees 24px-top/8px-bottom around the
  label exactly as rendered. Between-item-card spacing is unchanged.
- tsc: clean. lint: clean. vitest: 48 passed, 8 skipped (unchanged).
  `npm run build` verified clean.

ROUND 154 — MACRO BADGE SIZE, MEAL-CARD BORDER TOKEN, EXACT PIXEL SPACING (2026-07-04)
Files: src/ui/kit/MacroSummaryLine.tsx, src/ui/components/PantryMealDetail.tsx,
       src/ui/components/AddEntrySheet.tsx
- Macro badges: text stepped down to text-footnote (13px, was text-subhead
  15px). Meal summary card border switched to border-border-card-no-shadow
  (no literal "border-hover" token exists; this is the documented token for
  a flat, no-shadow card — the exact category this card is in since round
  153). Kcal badge next to the meal-name field now bg-surface-sunken,
  matching the name input's own background.
- Spacing made exact via inline styles instead of relying on the ambient
  space-y utility, after Marco reported the rendered image-to-card gap was
  only ~4px (not the 16-24px space-y implied) and inconsistent between the
  Day's-log and Pantry views. Summary card: `marginTop: '24px'` inline, same
  in both views. "Food items" heading + its item list share a small wrapper
  so the heading's own 8px bottom margin doesn't collapse against the first
  item card's auto margin-top — guarantees 24px-top/8px-bottom around the
  label exactly as rendered. Between-item-card spacing is unchanged.
- tsc: clean. lint: clean. vitest: 48 passed, 8 skipped (unchanged).
  `npm run build` verified clean.

ROUND 153 — MACRO BADGE POLISH, MEAL-CARD OUTLINE, SHADOW REVERT (2026-07-04)
Files: src/ui/kit/MacroSummaryLine.tsx, src/ui/kit/Sheet.tsx,
       src/ui/components/PantryMealDetail.tsx, src/ui/components/AddEntrySheet.tsx
- Follow-up to round 152's feedback: macro badges now bg-surface-muted
  (lighter than bg-surface-sunken) + rounded-[8px] (was 14px); text size
  already matched his 15pt spec exactly (text-subhead = 0.9375rem = 15px),
  no change needed. Meal summary card switched from a flat sunken fill to
  an outlined card (border + bg-surface, no shadow) on both the Day's-log
  Meal view and Pantry's Meal detail; "Food items" label bumped to
  text-headline (18px).
- Sheet.tsx's round-152 scroll-shadow changes were FULLY REVERTED — Marco
  reported the fix didn't work and introduced a new bug. Confirmed via git
  diff that the shadow edit was Sheet.tsx's only change since round 151, so
  restoring that exact prior file was a clean, complete revert. The
  original shadow issue (see round 152's entry / CHANGELOG) is still open.
- tsc: clean. lint: clean. vitest: 48 passed, 8 skipped (unchanged).
  `npm run build` verified clean.

ROUND 153 — MACRO BADGE POLISH, MEAL-CARD OUTLINE, SHADOW REVERT (2026-07-04)
Files: src/ui/kit/MacroSummaryLine.tsx, src/ui/kit/Sheet.tsx,
       src/ui/components/PantryMealDetail.tsx, src/ui/components/AddEntrySheet.tsx
- Follow-up to round 152's feedback: macro badges now bg-surface-muted
  (lighter than bg-surface-sunken) + rounded-[8px] (was 14px); text size
  already matched his 15pt spec exactly (text-subhead = 0.9375rem = 15px),
  no change needed. Meal summary card switched from a flat sunken fill to
  an outlined card (border + bg-surface, no shadow) on both the Day's-log
  Meal view and Pantry's Meal detail; "Food items" label bumped to
  text-headline (18px).
- Sheet.tsx's round-152 scroll-shadow changes were FULLY REVERTED — Marco
  reported the fix didn't work and introduced a new bug. Confirmed via git
  diff that the shadow edit was Sheet.tsx's only change since round 151, so
  restoring that exact prior file was a clean, complete revert. The
  original shadow issue (see round 152's entry / CHANGELOG) is still open.
- tsc: clean. lint: clean. vitest: 48 passed, 8 skipped (unchanged).
  `npm run build` verified clean.

ROUND 152 — SIX-PART UI/UX BATCH: BADGES, BOLD NAMES, MEAL SUMMARY CARD, SHADOWS, DUPES (2026-07-04)
Files: src/domain/pantry.ts, src/ui/kit/MacroSummaryLine.tsx, src/ui/kit/Sheet.tsx,
       src/ui/components/AddEntrySheet.tsx, src/ui/components/PantryMealDetail.tsx,
       src/ui/components/PantryFoodItemDetail.tsx, src/ui/components/PantryItemCard.tsx,
       src/ui/components/PantryPicker.tsx, src/ui/components/PantryNewFoodScan.tsx,
       src/ui/screens/PantryScreen.tsx, src/ui/screens/TodayScreen.tsx
- Six independent items validated point-by-point with Marco before building:
  (1) "+ New food" method cards moved up 16px, compensated with a 16px
  spacer so the sheet's total height is unchanged. (2) MacroSummaryLine
  (shared by BasketCard/PantryItemCard) now renders 4 individual badges
  (px-2 py-0.5 each) instead of one long dot-separated pill. (3) Bold food
  name / regular kcal (was backwards) across every card pairing the two —
  Basket card, Pantry item card, Pantry list row, both pantry-picker
  variants, Diary log row. (4) Long-standing meal-summary TODO (since round
  123) resolved on BOTH Day's-log's "Meal" view (LogEntrySheet/
  LogEntryContent — also gained a date subtitle it was missing) and
  Pantry's own Meal detail (same summary card, minus the Save-to-pantry
  checkbox and minus any date — Marco was explicit: no date there, it's a
  dateless reusable template). (5) Two Sheet.tsx scroll-shadow bugs: the
  main header's shadow was disappearing under scrolled content because the
  header had no background of its own (fixed: explicit bg-surface);
  OverlayLayer's nav bar had a stray shadow bleeding onto the grab handle
  above it from a `-mt-1` overlap (fixed: removed the negative margin).
  (6) Duplicate pantry names from repeat scans (same photo -> same
  AI-suggested name every time, no check before committing) — added
  uniquePantryName() (auto-suffixes "(2)", "(3)"...) to domain/pantry.ts,
  wired into every scan-driven Food-item/Meal creation path; left Manual/
  pick-existing paths alone (not what was reported, already a deliberate
  user action each time).
- tsc: clean. lint: clean. vitest: 48 passed, 8 skipped (unchanged).
  `npm run build` verified clean.

ROUND 152 — SIX-PART UI/UX BATCH: BADGES, BOLD NAMES, MEAL SUMMARY CARD, SHADOWS, DUPES (2026-07-04)
Files: src/domain/pantry.ts, src/ui/kit/MacroSummaryLine.tsx, src/ui/kit/Sheet.tsx,
       src/ui/components/AddEntrySheet.tsx, src/ui/components/PantryMealDetail.tsx,
       src/ui/components/PantryFoodItemDetail.tsx, src/ui/components/PantryItemCard.tsx,
       src/ui/components/PantryPicker.tsx, src/ui/components/PantryNewFoodScan.tsx,
       src/ui/screens/PantryScreen.tsx, src/ui/screens/TodayScreen.tsx
- Six independent items validated point-by-point with Marco before building:
  (1) "+ New food" method cards moved up 16px, compensated with a 16px
  spacer so the sheet's total height is unchanged. (2) MacroSummaryLine
  (shared by BasketCard/PantryItemCard) now renders 4 individual badges
  (px-2 py-0.5 each) instead of one long dot-separated pill. (3) Bold food
  name / regular kcal (was backwards) across every card pairing the two —
  Basket card, Pantry item card, Pantry list row, both pantry-picker
  variants, Diary log row. (4) Long-standing meal-summary TODO (since round
  123) resolved on BOTH Day's-log's "Meal" view (LogEntrySheet/
  LogEntryContent — also gained a date subtitle it was missing) and
  Pantry's own Meal detail (same summary card, minus the Save-to-pantry
  checkbox and minus any date — Marco was explicit: no date there, it's a
  dateless reusable template). (5) Two Sheet.tsx scroll-shadow bugs: the
  main header's shadow was disappearing under scrolled content because the
  header had no background of its own (fixed: explicit bg-surface);
  OverlayLayer's nav bar had a stray shadow bleeding onto the grab handle
  above it from a `-mt-1` overlap (fixed: removed the negative margin).
  (6) Duplicate pantry names from repeat scans (same photo -> same
  AI-suggested name every time, no check before committing) — added
  uniquePantryName() (auto-suffixes "(2)", "(3)"...) to domain/pantry.ts,
  wired into every scan-driven Food-item/Meal creation path; left Manual/
  pick-existing paths alone (not what was reported, already a deliberate
  user action each time).
- tsc: clean. lint: clean. vitest: 48 passed, 8 skipped (unchanged).
  `npm run build` verified clean.

ROUND 151 — BRING BACK THE "CHANGE" BUTTON ON SCAN-CAPTURED PANTRY ITEMS (2026-07-04)
Files: src/ui/components/PantryItemCard.tsx, src/ui/components/PantryFoodItemDetail.tsx,
       src/ui/components/PantryMealDetail.tsx, src/ui/screens/PantryScreen.tsx
- Marco: round 149 removing the pre-commit review screen also took the
  round-148 "Change" button down with it (nowhere left to attach it to).
  Reintroduced it on the POST-commit item cards instead, scoped to the same
  transient "just happened" window Change always had (never a permanent
  per-item flag): PantryItemCard gained an `onCorrect` prop (same pill as
  BasketCard's). PantryFoodItemDetail shows it on its own card while
  `justCreated` (round 150) is true — tapping it re-describes and
  overwrites that exact FoodItem in place, or converts to a Meal if the
  correction yields 2+ foods (1-vs-2+ rule), deleting the now-superseded
  single item (safe — nothing could reference it yet). PantryMealDetail
  tracks a `scannedItemIds` set, seeded from a new `justCreatedItemIds`
  prop (whole Meal just arrived from a scan) and grown as more items are
  scanned in via "Add another item" while the Sheet stays open; "Change" on
  one of those splices the correction into meal.items in that ingredient's
  place, leaving the superseded FoodItem orphaned (same as removing an item
  from a meal already does).
- Also: Marco decided to leave the Nutri-scan-also-reads-whole-food
  question (round 150's TODO) alone for now — not pursuing it.
- tsc: clean. lint: clean. vitest: 48 passed, 8 skipped (unchanged).
  `npm run build` verified clean.

ROUND 151 — BRING BACK THE "CHANGE" BUTTON ON SCAN-CAPTURED PANTRY ITEMS (2026-07-04)
Files: src/ui/components/PantryItemCard.tsx, src/ui/components/PantryFoodItemDetail.tsx,
       src/ui/components/PantryMealDetail.tsx, src/ui/screens/PantryScreen.tsx
- Marco: round 149 removing the pre-commit review screen also took the
  round-148 "Change" button down with it (nowhere left to attach it to).
  Reintroduced it on the POST-commit item cards instead, scoped to the same
  transient "just happened" window Change always had (never a permanent
  per-item flag): PantryItemCard gained an `onCorrect` prop (same pill as
  BasketCard's). PantryFoodItemDetail shows it on its own card while
  `justCreated` (round 150) is true — tapping it re-describes and
  overwrites that exact FoodItem in place, or converts to a Meal if the
  correction yields 2+ foods (1-vs-2+ rule), deleting the now-superseded
  single item (safe — nothing could reference it yet). PantryMealDetail
  tracks a `scannedItemIds` set, seeded from a new `justCreatedItemIds`
  prop (whole Meal just arrived from a scan) and grown as more items are
  scanned in via "Add another item" while the Sheet stays open; "Change" on
  one of those splices the correction into meal.items in that ingredient's
  place, leaving the superseded FoodItem orphaned (same as removing an item
  from a meal already does).
- Also: Marco decided to leave the Nutri-scan-also-reads-whole-food
  question (round 150's TODO) alone for now — not pursuing it.
- tsc: clean. lint: clean. vitest: 48 passed, 8 skipped (unchanged).
  `npm run build` verified clean.

ROUND 150 — "+ NEW FOOD": ONE PERSISTENT SHEET INSTEAD OF A SECOND BLANK ONE; "SAVE FOOD" CTA (2026-07-04)
Files: src/ui/components/PantryNewFoodScan.tsx (rewritten), src/ui/screens/PantryScreen.tsx,
       src/ui/components/PantryFoodItemDetail.tsx
- Marco (two screenshots): tapping Camera/Photo/Describe/Nutri-scan from
  "+ New food" opened a SECOND, blank, full-screen sheet on top of the
  compact "New food" method picker, instead of the picker staying put with
  a loader in place. Root cause: PantryScreen tracked `pickingMethod` and
  `scanMethod` as two separate states, each rendering its OWN Sheet
  (MethodPickerModal, then the old forceExpanded PantryNewFoodScan) — the
  hand-off between them read as a flash of an empty full-height sheet, and
  for Nutri-scan it appeared behind the native iOS photo-picker action
  sheet too. Fixed by collapsing the whole flow into one `PantryNewFood`
  component: a SINGLE, compact (non-forceExpanded) Sheet for its entire
  lifetime, swapping only its inner content (picker -> spinner/Describe ->
  commit). Manual is unaffected — still hands off to its own separate,
  forceExpanded form Sheet. Describe's textarea renders as a plain Sheet
  child rather than via useSheetSetOverlay (every other call site in the
  app pairs that with forceExpanded; this Sheet must stay compact) —
  DescribeOverlay's OverlayNav/useOverlaySetFooter calls no-op safely
  without that context, so the only loss is the "< Describe" sub-header row
  (the Sheet's own title + X already covered cancel).
- Also added a `justCreated` prop to PantryFoodItemDetail so the CTA reads
  primary "Save food" (not outline "Close") when landed on right after a
  scan commit — those have no prior explicit save tap, unlike Manual.
  PantryMealDetail needed no change (CTA was already unconditional "Save
  meal").
- NOT done: unifying the per-100g/per-serving prompt across all 4 methods
  (Marco's "Nutri-scan also reads whole food" observation) — not
  mechanically detectable from the current scan API response without
  guessing; flagged back to Marco with concrete options instead.
- tsc: clean. lint: clean. vitest: 48 passed, 8 skipped (unchanged).
  `npm run build` verified clean.

================================================================
TODO / FUTURE WORK
================================================================
- "+NEW FOOD" LOADING + TRANSITION POLISH: round 150 fixed this for the
  Camera/Photo/Describe/Nutri-scan methods — they now live inside ONE
  persistent Sheet with phase-based content (picker -> spinner/Describe ->
  commit), no more sheet-swap flash. NOT yet applied to Manual: tapping
  Manual still hands off to a separate forceExpanded form Sheet, and the
  New-food-form -> Food-item-detail transition there still slides one sheet
  down then a new one up rather than happening "in place" — Marco's
  original ask was for a dedicated ~2s "Saving food" loading screen there
  too, still outstanding.
- NUTRI-SCAN vs. CAMERA/PHOTO/DESCRIBE consistency: raised in round 150
  (Nutri-scan also reads whole food, not just labels, making its
  always-ask-per-100g/serving behaviour feel inconsistent with the other 3
  methods) — Marco decided (round 151) to leave this alone for now.
- BASKET-VIEW UNIFICATION (partially done, round 124): DeleteIcon/EditIcon and
  AddAnotherSection/MethodCards are now genuinely shared (icons.tsx,
  MethodCards.tsx) between the Day's-log basket and every Pantry surface —
  one change to either reflects everywhere. Still intentionally NOT unified:
  PantryItemCard vs BasketCard themselves stay separate components (opposite
  stepper rules — Pantry disabled vs Day's-log enabled — and different trash
  semantics). Worth a dedicated round to decide how far a full "one card,
  many contexts" unification should go, rather than doing it as a side-effect
  of an unrelated change.
- KEYBOARD TOOLBAR (accessory bar): round 160 closed the "no closing
  action" half of this gap — every numeric-keyboard input now shows a
  "Done" bar (useNumericDoneBar). Still open: prev/next field navigation
  between inputs inside a multi-field sheet (GoalSetupScreen Section 2,
  ProfileSetupSheet, EditFoodSheet) — Capacitor InputAccessoryView or a
  custom UIToolbar injected via WKWebView bridge would be needed for that.
