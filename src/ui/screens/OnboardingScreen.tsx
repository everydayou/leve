import { FirstOpenForkScreen } from './FirstOpenFork';

/** First-open screen. Replaced the old carousel + profile step with the
 *  "What brings you here?" fork per FIRST-OPEN-FLOW-CONCEPT.md. */
export function OnboardingScreen() {
  return <FirstOpenForkScreen />;
}
