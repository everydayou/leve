================================================================
NUTRITION GOAL TRACKER — PROJECT CONTEXT
================================================================
Owner: Marco (marcosilva86@gmail.com)
Last updated: 2026-07-08, Round 191 (TestFlight distribution set up;
Apple Health integration shipped for weight + activity, real on-device
sync via @capgo/capacitor-health, no backend; bring-your-own Claude API
key added for AI food scan, stored in iOS Keychain, calls Anthropic
directly when set; every AI-feature trigger now gated with an upfront
"Connect AI to use this" check instead of reactive error handling;
Account's Tracking and Settings moved into their own full-screen
sub-views under a new Options menu, with swipe-back and a shared muted
background)
Full change history: see CHANGELOG.txt (rounds 1-191).

HOW TO READ THIS DOC:
- This file = current state only. Read it at the start of every chat.
- CHANGELOG.txt = full per-round history. Open it when you need to know
  WHY something was decided or what changed in a specific round.
- Before touching any UI: read section 0B (Design-System Contract) first.
- If two statements conflict: the higher round number wins.

----------------------------------------------------------------
0. QUICK STATUS
----------------------------------------------------------------
- App is RUNNING on Marco's iPhone (native Capacitor build, free Apple ID).
  On-device install: DONE as of 2026-06-04 (runbook: INSTALL-iOS.md).
- THREE PREVIEW SURFACES, each with its OWN separate data store:
  (1) iPhone native app (Capacitor) — Marco's REAL daily-use data; IndexedDB
      in the app container; persists across rebuilds/re-signing.
  (2) Browser dev server (`npm run dev` -> localhost) — real app with
      hot-reload; IndexedDB local to that browser; SEPARATE from the phone.
  (3) Double-click demo HTML (file://) — in-memory, resets on refresh.
  Data does NOT sync between surfaces.
- V1 FEATURE STATUS: feature-complete and running on phone.
- VERIFY-ON-MAC REMINDER: after Cowork changes, run on Mac:
  `npm install` → `npm test` → `npm run build` → `npm run ios:sync`.

----------------------------------------------------------------
0B. DESIGN-SYSTEM CONTRACT — FIXED RULES (read BEFORE editing any UI)
----------------------------------------------------------------
Non-negotiable standing rules. Any chat touching src/ui MUST follow them.

1. COMPONENTS FIRST. Build UI from kit atoms in src/ui/kit: Button/IconButton,
   Sheet, LabeledInput, NumberField, Card/Surface, StatTile, ProgressBar/
   GaugeArc, Badge, SegmentedControl, FilterPills, Icon, etc. NEVER hand-roll
   a button/input/sheet/stat inline when a kit atom exists.
2. TOKENS ONLY. Colour, radius, shadow and type from src/index.css semantic
   tokens. NEVER raw Tailwind text sizes (text-xs/sm/base/lg/xl) — use
   text-caption/footnote/subhead/body/callout/headline/title/display/hero.
   NEVER hardcode hex/rgb — use colour tokens.
3. ICONS, NOT GLYPHS. Use <Icon> for all affordances. Never type ›, ‹ or ×.
4. ONE SOURCE OF TRUTH. Change look via token or atom, never per-screen CSS.
5. ENFORCEMENT. eslint.config.js BLOCKS raw text sizes and hex/rgb in src/ui.
   Run `npm run lint` before shipping. Do NOT disable guardrails.
6. ACCESSIBILITY FIRST. Focus outlines, ARIA roles, prefersReducedMotion(),
   WCAG AA contrast, 44×44pt touch targets, labeled form inputs.

Text scale: caption(12) / footnote(13) / subhead(15) / callout(16) /
headline(18) / title(22) / display(30) / hero(48).
WHERE THINGS LIVE: tokens = src/index.css (@theme); atoms = src/ui/kit/;
live reference = /styleguide route.

----------------------------------------------------------------
1. WHAT WE'RE BUILDING
----------------------------------------------------------------
A personal nutrition + weight-management app for Marco's own use.
NOT a calorie tracker. Core idea: GOAL MANAGEMENT — tracking exists only
to answer "Am I on track to hit my goal?"
Mental model: Goal → Weekly progress → Daily progress → Food/Activity inputs.

----------------------------------------------------------------
2. PLATFORM & STACK
----------------------------------------------------------------
React + Capacitor (web code + native iOS shell). NOT native Swift.
Stack: Vite 6, React 19, TypeScript, Tailwind CSS v4, Dexie (IndexedDB),
Vitest. Capacitor v8. appId: com.marco.nutritiongoal, webDir: dist.
Install path: Xcode + free Apple ID ($0). Re-sign every ~7 days.

----------------------------------------------------------------
3. AGREED V1 SCOPE
----------------------------------------------------------------
- Goal types: lose_by_date and gain_by_date.
- TODAY: frequent foods, Add-entry sheet, calories + protein, deficit,
  swipeable day nav.
- GOAL: weight trend vs. target line, 9 lifecycle states.
- PANTRY: create/edit food items.
- Manual meal entry, manual activity calories, weight logging.
- BMR: single user-entered number in V1.
- Account: weight history, profile, BMR, export (JSON).
DEFER (V1.5+): BMR auto-calibration, HealthKit/Withings, History tab,
per-piece measurement types.

----------------------------------------------------------------
4. NAVIGATION / IA
----------------------------------------------------------------
- Bottom tab bar: Today / Goal / Pantry / Account.
- Center "+" FAB speed dial: Scan / Food / Activity / Weight.
  Scan item only shown when VITE_FOOD_SCAN_API_URL is set (SCAN_ENABLED gate).
- Add-entry = bottom sheet (never full-screen nav).
- Goal Setup = full-screen flow. ?skip-type=true skips plan-type step.
- Day nav on Today: chevrons + swipe. Future dates disabled.

----------------------------------------------------------------
4B. V1 DATA MODEL
----------------------------------------------------------------
Goal status: active / completed / abandoned.
GoalScreen renders 9 states (all implemented r55):
  (1) no-goal  (2) active-normal  (3) final-day·no-weight
  (4) final-day·weight-in(not hit)  (5) final-day·goal-hit
  (6) early-complete  (7) overdue  (8) completed  (9) abandoned/ended
GoalOutcomeView: createPortal → document.body. Slide-up on mount.
GoalSettingsSheet (⋯): Mark complete / Edit plan / End goal / Past goals.
User model: weightCadence ('daily'|'weekly'), weeklyWeightDay.

----------------------------------------------------------------
5. METRICS / MATH
----------------------------------------------------------------
Total Burn = BMR + Active Calories.
Daily Deficit = Total Burn − Calories Consumed.
~7700 kcal per kg for goal pace math.

----------------------------------------------------------------
7. CURRENT STATUS (as of round 191, 2026-07-08)
----------------------------------------------------------------
All V1 features shipped and running on device. This is a snapshot only.
Detailed round-by-round history lives in CHANGELOG.txt (rounds 1-191)
and the 3 most recent rounds are kept in full in RECENT ROUNDS below.

- THREE GOAL TYPES NOW SHIPPED (round 190): maintain_weight joins
  lose_by_date/gain_by_date end-to-end — GoalSetupScreen (Guided band
  presets + Detailed custom range picker), GoalScreen/TodayScreen/
  PastGoalsScreen all branch three ways, FirstOpenFork/GoalForkScreen
  offer it as a path card. Gain and maintain share their band-gauge/chart
  logic via a new usesKcalBand() helper. See RECENT ROUNDS for full detail.
- KEYBOARD "DONE" BAR GAP ROOT-CAUSED (round 190, see KNOWN GAPS for
  confirmation status): a Done bar rendered inside a Sheet's nested
  overlay (OverlayLayer) was positioning against that overlay's own
  transformed box instead of the true viewport — fixed by portaling the
  bar straight to document.body. A first, wrong hypothesis (compensating
  for env(safe-area-inset-bottom)) was tried, made things worse, and was
  reverted before the real cause was found via on-device diagnostics.
- GOAL-FORK NAVIGATION FIXED (round 190, device-confirmed): going back
  from Goal-setup to the goal-type fork screen no longer shows a blank
  flash before the destination appears, and no longer replays its
  slide-up entrance animation on every return visit.
- ACCOUNT SCREEN REDESIGNED (round 180): Profile+BMR merged into the same
  gauge-card layered pattern as Diary/Today (white profile card floats on
  a grey reveal; BMR lives in the grey area — label, icon, then value +
  "kcal / day", 16pt), now at 24px radius (rounded-sheet). Whole card
  opens Edit profile; "Edit" moved above the card as a 15pt accent-hover
  text label (no background). Weight-units toggle moved out of the
  Edit-profile sheet into its own live Tracking card (WeightUnitsCard);
  Edit-profile's Weight field now reuses the same WeightLogSheet as
  Today/Goal ("tap the field" — no separate button), confirmed to
  upsert-by-date so it always reflects the most recent calendar entry.
  Goal and Connections sections are hidden, not deleted
  (SHOW_GOAL_SECTION / SHOW_CONNECTIONS_SECTION consts in
  AccountScreen.tsx) — Goal duplicated the Goal tab, Connections
  (Withings) isn't needed yet. Data (export JSON) moved under a new
  Developer section, hidden by default and revealed by triple-tapping
  the "Account" title; the reveal persists across reloads via new
  src/lib/devReveal.ts (localStorage) and toggles off the same way.
  Tracking/Settings cards (Weight units, Weigh-in frequency, Diary
  macros, Theme/Haptics) restyled to match GoalScreen's own outline-card
  pattern: rounded-card (16px), white fill, inset border-field hairline,
  no drop shadow; titles bold/16pt/text-content. Selected weigh-in day
  pill now uses the shared segmented-active look (white + shadow +
  accent-hover text) instead of a solid fill; its footer sentence spells
  out full day names ("Mondays") instead of abbreviated + "s".
- ACCOUNT RESTRUCTURED (round 191): Tracking and Settings are their own
  full-screen sub-views (slide in from the right, back chevron, plus a
  swipe-right gesture to go back), reached from a small "Options" section
  (two-row menu) below Profile on the main Account view instead of being
  shown inline. Both sub-views share Account's own muted page background
  instead of a plain white sheet. SlideScreen/SlideHeader were extracted
  from PastGoalsScreen.tsx into src/ui/kit/SlideScreen.tsx (two new
  opt-in props, onBack and muted, both additive) so Account and Past
  goals share one implementation. Developer stays nested inside the
  Settings sub-view (still gated by the same triple-tap-title reveal),
  not its own menu row.
- APPLE HEALTH INTEGRATION SHIPPED (round 191): real, on-device sync of
  weight and activity/active-energy via @capgo/capacitor-health, no
  backend. Connect/disconnect lives in the Settings sub-view; sync runs
  on app open/foreground plus on-demand, is clamped to never backfill
  before the moment Connect was tapped, and never overwrites a manually
  logged entry for the same day. A synced Activity entry can be hidden
  (excluded from totals, stays visible muted in the day's log) or
  refreshed on demand. See HEALTHKIT.md for the one manual Xcode step
  (HealthKit capability) required after a fresh clone.
- BRING-YOUR-OWN CLAUDE API KEY + AI CONNECTION GATE (round 191): Account
  > Settings > AI Food Scan lets a user store their own Anthropic API key
  (iOS Keychain via @aparajita/capacitor-secure-storage); when set,
  lib/foodScan.ts calls api.anthropic.com directly instead of the shared
  Vercel proxy. Every AI-feature trigger (Camera/Photo/Describe/
  Nutri-scan/Fix, across Day's log and all Pantry surfaces) is gated
  upfront via useAiGate: no key cached shows a short "Connect AI to use
  this" sheet before the feature ever opens, instead of waiting for a
  scan to fail. Reactive error handling (FoodScanError) is kept as a
  second layer for genuine failures (rejected key, network/proxy issues)
  with a tappable action back into the key sheet.
- TESTFLIGHT DISTRIBUTION SET UP (round 191): app icon alpha channel and
  a bundle-ID mismatch fixed; TESTFLIGHT.md documents the full
  archive-and-upload runbook. The existing cable-run `npm run ios` /
  Xcode ▶ Run workflow is unchanged and stays the fast day-to-day path;
  TestFlight is a separate, occasional channel.
- SegmentedControl replaced with FilterPills for Account's sex/weight
  units/weigh-in frequency toggles (round 191), matching the pattern
  Goal Setup already used.
- Diary/Today gauge-card + macro-bars layered pattern (rounds 162-165) is
  now also the shared reference pattern for the 3 Basket Meal views
  (PantryMealDetail, FoodForm, LogEntryContent): a white meal-summary
  card floats on a grey Food-items panel below it.
- Grey Food-items panel fill CONFIRMED working on-device (round 170's
  bottomGapPx cap on top of round 168's root-cause fix) — no further
  reports since. The app-freeze bug on X -> discard-confirm -> "Keep
  editing" is also confirmed fixed on-device (round 171,
  closeImmediately race condition in Sheet.tsx).
- PANTRY now ships with 150 default ("origin:'app'") Food items, seeded
  once per device on first load after this update (round 179,
  src/data/defaultPantry.ts + defaultPantrySeed.ts) — common staples
  across proteins/grains/fruits/veg/dairy/fats, per-100g macros, no
  images. Their measurement type + macro fields are locked/greyed-out in
  FoodItemForm (name/photo stay editable). PantryScreen and PantryPicker
  both now filter via 3 independent toggle pills — Food items / Meals /
  My own (round 177-178) — replacing the old single-select All/Food
  items/Meals radio; "My own" restricts to origin:'user' and is disabled
  whenever the view is Meals-only (no app-provided Meals exist).
- Default placeholder image: src/assets/food-placeholder.png (569KB,
  round 168, unchanged — a round 174 attempt to recolor it was reverted
  round 175). Image corner radius is 32px everywhere a photo or
  placeholder renders (round 168), matching radius-main. Real photos
  (not the placeholder) now use a dedicated, darker --shadow-photo token
  (round 176) instead of the shared shadow-card-lg.
- Carb targets for Balanced/Performance macro styles scale with body
  weight + today's actual activity (activityCarbTargetG, round 169),
  replacing the old "whatever's left after protein/fat" residual formula.
  Baseline g/kg and activity-share constants are initial estimates —
  see KNOWN GAPS.
- .env.local: gitignored, must be recreated if folder is deleted.
  VITE_FOOD_SCAN_API_URL=https://food-scan-api-delta.vercel.app
- tsc: clean. lint: clean. vitest: 57 passed, 8 skipped.

----------------------------------------------------------------
8. ARCHITECTURE NOTES
----------------------------------------------------------------
Layer model (strict dependency direction):
  src/domain/   Pure TS — types.ts, calc.ts, goal.ts, bmr.ts.
  src/data/     Dexie + memory repos. repositories.ts = interfaces.
  src/state/    repos.ts (swap point) + useLive() + useDay().
  src/ui/       AppShell + screens + components + kit/.
  src/index.css Design token layer (Tailwind v4 @theme).

KEY PATTERNS:
- createPortal: GoalOutcomeView renders into document.body to escape
  AppShell overflow-hidden / z-index on WKWebView.
- slide-up/down: .slide-up-in / .slide-down-out in index.css.
  Exit: setExiting(true) → setTimeout(cb, 320) → navigate.
- Sheet-on-Sheet: destructive confirms render a second Sheet alongside
  the parent in a Fragment (both conditionally mounted).
- SCAN_ENABLED = !!(import.meta.env.VITE_FOOD_SCAN_API_URL) — used in
  AddEntrySheet.tsx header icon and FloatingTabBar.tsx FAB action list.
  Unaffected by bring-your-own-key (below); gates whether scan exists
  at all, not which backend handles a request.
- Bring-your-own Claude API key (Account > Settings > AI Food Scan,
  ApiKeySheet.tsx): user pastes their own Anthropic key, stored via
  @aparajita/capacitor-secure-storage (iOS Keychain / Android Keystore,
  lib/apiKey.ts). When set, lib/foodScan.ts's scanFood/describeFood call
  api.anthropic.com directly from the device (model: claude-sonnet-5,
  forced tool_use for structured output) instead of the shared Vercel
  proxy. This bypasses food-scan-api entirely, so no changes needed on
  that separate Vercel project.
- useAiGate (src/ui/components/useAiGate.tsx): every AI-feature trigger
  (Camera/Photo/Describe/Nutri-scan, incl. "Fix"/re-describe) is gated on
  tap. No personal key cached shows a short "Connect AI to use this" sheet
  (View more -> ApiKeySheet, or Cancel) instead of proceeding, checked
  BEFORE opening the camera/picker/describe overlay at all. This means the
  shared Vercel proxy fallback in lib/foodScan.ts is effectively
  unreachable from normal UI flow now (every entry point requires a key
  first), kept as defense-in-depth, not deleted. lib/apiKey.ts's in-memory
  key cache is warmed once on boot (AppShell) so the gate's tap-time check
  is synchronous.

ENV NOTE: Cowork Linux sandbox can't run rollup native binary. After
changes, on the Mac: `npm install` → `npm test` → `npm run build` →
`npm run ios:sync`.

----------------------------------------------------------------
KNOWN GAPS
----------------------------------------------------------------
HIGH:
- (none currently open — both former HIGH items were resolved in round
  181; see DONE (r181) below.)

MED:
- NO AUTOMATED TEST COVERAGE FOR HEALTHKIT/BYOK/GATE LOGIC (round 191,
  new): the HealthKit sync (dedup/clamp/hide-in-place), the direct-to-
  Anthropic BYOK call path, and the useAiGate upfront check were all
  verified manually and on-device only, not via vitest. Worth adding
  unit coverage for the pure logic pieces (queryAggregated bucketing,
  connectedAt clamp, FoodScanError classification) if this area sees
  more churn.
- DONE-BAR PORTAL FIX AWAITING FINAL ON-DEVICE CONFIRMATION (round 190,
  new): the Done-bar gap (open since round 181, "still a bit high" on
  fields like Edit food item's Name) was root-caused this round to
  position:fixed elements inside a transformed OverlayLayer ancestor not
  actually anchoring to the true viewport — fixed via createPortal to
  document.body. Strongly evidenced via an on-device A/B (Dev > Keyboard
  playground row 5, plain top-level field: pixel-perfect; the same hook
  nested in an overlay: gap present), but the ORIGINAL repro case (Name
  field in Edit food item) hasn't yet been re-tested against this exact
  fix. A first, unrelated hypothesis this round (compensating for
  env(safe-area-inset-bottom)) made the gap dramatically worse and was
  reverted — see ROUND 190 below before assuming any keyboard-inset
  related fix is safe to retry blindly.
- CARB TARGET MODEL TUNING (round 169; capped round 181): activityCarbTargetG's
  constants (2.2g/kg Balanced, 2.4g/kg Performance baseline; 55%/75%
  share of active kcal) are initial estimates, not yet validated against
  Marco's real day-to-day numbers — likely needs tuning after a few days
  of use (see CARB_MODEL in domain/goal.ts to adjust). Round 181 added a
  4.5g/kg hard cap for Balanced only (Performance stays uncapped) to stop
  the ~400g/day outlier on high-activity days — the cap value itself is
  also unvalidated.
- RETROACTIVE MACRO-CORRECTION RISK (round 179, new): nutrition is always
  computed LIVE from a Food item's current fields (nutritionFor in
  calc.ts) — never snapshotted at the moment something's logged. This
  predates round 179 but matters much more now: any future correction to
  one of the 150 seeded default-pantry values would silently change the
  numbers on every past diary entry that referenced it, for everyone who
  logged it. Accepted trade-off for now (see round 179's CHANGELOG entry)
  — revisit if a real correction is ever needed post-launch.
- DEFAULT-PANTRY SEED IS V1-ONLY (round 179, new; per-profile bug fixed
  round 190): defaultPantrySeed.ts seeds once per device via a
  localStorage flag, now scoped per real/test profile
  (defaultPantrySeedFlagKey) after round 190 fixed real-account seeding
  permanently blocking test-account seeding (and vice versa). The
  original gap still stands though: adding new default foods or
  correcting existing ones later needs its own new versioned flag/
  migration step (same pattern as mealsMigration.ts) — nothing currently
  re-syncs an already-seeded profile.
- PLACEHOLDER IMAGE SIZE (round 168): food-placeholder.png is 569KB
  (down from round 166's 812KB) but still sizeable for a UI placeholder
  rendered at 256×256 — worth compressing further if bundle size becomes
  a concern; not blocking.
- NO CONFIRM on destructive deletes of food entries, pantry items, weight
  entries. (Goal actions confirm via Sheet; entry deletes are immediate.)
- .env.local is gitignored and must be recreated manually if the project
  folder is deleted. Contains VITE_FOOD_SCAN_API_URL (Vercel scan API URL).
- PROFILE CARD NESTED INTERACTIVE CONTROL (round 180, new): the Profile
  card is a div[role=button] wrapping the whole card to open Edit profile;
  the BMR info icon inside is a real nested <button> that stopPropagation
  s to open its own "About BMR" sheet instead. Works fine and is
  keyboard-activatable, but nested interactive controls are a minor a11y
  smell — revisit if VoiceOver testing surfaces an issue.

LOW:
- OUTLINE-CARD STYLE DUPLICATED (round 180, new): the rounded-card + inset
  border-field hairline, no-shadow card style Marco asked for on Account
  (Weight units/Weigh-in frequency/Diary macros/Theme, via the local
  OutlineCard helper in AccountScreen.tsx) is hand-rolled independently of
  GoalScreen.tsx's goal-overview container, which uses the same inset-
  border technique on its own. Worth extracting into a shared kit atom if
  a 3rd screen wants it — left as-is for now since only 2 screens use it.
- DEFAULT-PANTRY NAME-COLLISION CHECK IS EXACT-MATCH (round 179, new):
  defaultPantrySeed.ts skips a default only when its name exactly matches
  (case-insensitive, trimmed) an existing item. Near-duplicate names
  (e.g. a user's own "Chicken Breast" vs the seeded "Chicken breast,
  cooked") both appear as separate rows rather than merging — Marco
  flagged this explicitly; decided to leave as-is rather than add fuzzy
  matching (real false-positive risk, e.g. "chicken thigh" vs "chicken
  breast").
- DEFAULT-PANTRY ITEMS HAVE NO IMAGES (round 179, deliberate v1 scope):
  discussed sourcing real photos vs. a matching icon/illustration set for
  the 150 defaults; decided to skip images entirely for v1. Revisit if
  wanted later — see the round 179 discussion in this session's history
  for the icon-set vs. photography trade-offs considered.
- PANTRY FILTER PILLS DUPLICATED, NOT SHARED (round 177-178): the 3-pill
  Food items/Meals/My own toggle row is bespoke, separately implemented
  in both PantryScreen.tsx and PantryPicker.tsx (deliberately, so
  FilterPills' other single-select call sites aren't affected) — a future
  change to this behavior needs to be applied by hand in both places.
- GoalOutcomeView: no haptic on appearance.
- App.tsx still has /past-goals and /past-goals/:id stub routes (now unused;
  PastGoalsPortal is portal-based). Safe to remove eventually.
- ACTIVITY TEACHING MOMENT (§9 first-open spec): deferred.
- ProfileSetupSheet height select always shows cm. Low priority.
- Estimated BMR (weight-only) assumes pop-avg 170 cm / 35 yr / neutral sex.
- STATUS-BAR TAP DEAD-ZONE (round 181, new, accepted): tapping exactly on
  the clock/time in the iOS status bar doesn't trigger the app's
  tap-to-scroll-to-top action — only taps slightly below it do. Tried an
  onTouchStart-based experiment (AppShell.tsx); Marco reported it "didn't
  really work" but it was left in place as harmless. Likely a WKWebView/
  native status-bar hit-testing limitation, not chased further.
- DONE_BAR_HEIGHT IS AN ESTIMATE, NOT DEVICE-MEASURED (round 181, new):
  the 40px constant (src/ui/kit/useKeyboardDoneBar.tsx) is derived from
  CSS padding/line-height math (16px py-2 + ~22.5px line-height for 15px
  text), not a pixel-measured on-device value. If the Done bar's type
  scale or padding ever changes, this constant needs updating in
  lockstep, or the keyboard-clearance math (Sheet.tsx, OverlayLayer,
  GoalSetupScreen.tsx) will drift.
- moreHoriz ICON NOW UNUSED (round 181, new, cleanup): Icon.tsx still
  exports a moreHoriz icon; nothing references it anymore now that the
  Goal-options button is a text label, not an icon. Safe to remove.

DONE (r191):
- ✓ APPLE HEALTH INTEGRATION (round 191): weight + activity sync shipped
  end-to-end, real on-device data via @capgo/capacitor-health. See
  CURRENT STATUS and ROUND 191 below for full detail.
- ✓ BRING-YOUR-OWN CLAUDE API KEY (round 191): personal Anthropic key,
  stored in iOS Keychain, routes AI food scan/describe directly to
  Anthropic instead of the shared proxy.
- ✓ AI CONNECTION GATE (round 191): every AI-feature trigger now checks
  for a key upfront instead of waiting for a scan to fail; also fixed
  the bug where removing a key still silently worked via the shared-
  proxy fallback.
- ✓ TESTFLIGHT DISTRIBUTION (round 191): app icon + bundle-ID fixes,
  TESTFLIGHT.md runbook added.
- ✓ ACCOUNT OPTIONS MENU (round 191): Tracking/Settings moved into
  full-screen sub-views under an Options menu, with swipe-back and a
  shared muted background.
- ✓ MAINTAIN-GOAL BADGE/TOTAL-DAYS BUGS FIXED (round 191): Diary badge
  and Past Goals' "Total days" now branch on isMaintainGoal() instead of
  assuming lose/gain fields exist.
- tsc: clean. lint: clean. vitest: 57 passed, 8 skipped.

DONE (r190):
- ✓ MAINTAIN-WEIGHT GOAL TYPE (round 190): shipped end-to-end across
  domain/goal.ts, GoalSetupScreen, GoalScreen, TodayScreen,
  PastGoalsScreen, AccountScreen, FirstOpenFork.tsx. See CURRENT STATUS
  and ROUND 190 below for full detail.
- ✓ GOAL-FORK NAVIGATION FIXED (round 190, device-confirmed): white-
  flash-then-pop-in on Goal-setup's exit, and the fork screen's
  slide-up entrance incorrectly replaying on every Back navigation, both
  fixed. See ROUND 190 below.
- ✓ SCROLLBAR CHROME HIDDEN APP-WIDE (round 190, device-confirmed):
  visible WKWebView scrollbar was poking through the keyboard's rounded
  corners; hidden globally via CSS.
- ✓ SINGLE-WHEEL WEIGHT PICKERS EVERYWHERE (round 190): the "picker
  playground picker 3" single-wheel style, previously Maintain-only, now
  also used for Lose-weight/Build-muscle's Start/Target weight fields.
- ✓ KEYBOARD DONE-BAR PORTAL FIX (round 190): see MED gap above for
  confirmation status — implemented and strongly evidenced, not yet
  re-confirmed against the original repro case.
- ✓ PANTRY SEED/MEALS MIGRATION PER-PROFILE (round 190): fixed real-
  account seeding permanently blocking test-account seeding; see MED
  gap above (DEFAULT-PANTRY SEED IS V1-ONLY) for the remaining, separate
  gap this doesn't address.
- tsc: clean. lint: clean. vitest: 57 passed (6 new), 8 skipped.

DONE (r181):
- ✓ KEYBOARD DONE BAR UNIFIED (round 181): useNumericDoneBar renamed to
  useKeyboardDoneBar and moved into LabeledInput, so every text field (not
  just numeric ones) gets the same "Done" dismiss accessory bar. Fixed an
  early regression where a forced 44px height made the bar look bigger
  than before — reverted to natural py-2 sizing (~40px, see KNOWN GAPS).
- ✓ BALANCED CARB TARGET CAP (round 181): activityCarbTargetG now applies
  a 4.5g/kg hard cap for the Balanced macro style only (Performance stays
  uncapped), fixing the ~400g/day outlier reported on high-activity days.
  See KNOWN GAPS (MED) — cap value itself still unvalidated.
- ✓ GOAL-OPTIONS AFFORDANCE (round 181): the "⋯" options icon in
  GoalScreen was first swapped for a custom Marco-supplied gear icon,
  then replaced again with a plain "Options" text label per follow-up
  feedback; the gear icon and its Icon.tsx entries were fully removed
  (moreHoriz remains defined but now unused — see KNOWN GAPS).
- ✓ 44×44PT CLEAR-BUTTON HIT AREAS (round 181): audited and fixed all
  three clear (X) button implementations (LabeledInput, Field,
  PantryScreen search bar) to meet the app's own touch-target rule —
  absolutely-positioned buttons sized directly, in-flow buttons via a
  padding + negative-margin trick that doesn't grow layout.
- ✓ GUIDED/DETAILED GOAL-SETUP RELABEL + MERGED INFO CARD (round 181):
  Simple/Custom segmented control renamed Guided/Detailed; added a
  mode-specific explanation card visually merged behind the segmented
  control (reviving the round-65 pattern). Fixed a follow-up bug where
  the sticky header's opaque background hid the card's top border — by
  moving the segmented control out of stickyHeader into normal scrolling
  flow (this also resolves the old "SHEET stickyHeader SLOT" HIGH gap,
  since that slot is no longer used for this control).
- ✓ TOP STATUS-BAR FADE GRADIENT (round 181): added a white gradient over
  the iOS notch/status-bar area app-wide (AppShell.tsx), mirroring the
  existing bottom-nav fade; content still scrolls through it unaffected,
  matching the pattern Marco approved. See KNOWN GAPS for the residual
  tap-to-scroll dead zone directly on the clock.
- ✓ KEYBOARD-COVERS-CONTENT BUG FIXED (round 181): OverlayLayer (used by
  "Edit food item" and other overlay sheets) was missing scroll-into-view
  logic entirely; added it (mirroring the main Sheet's pattern) and
  corrected the keyboard-clearance math app-wide to account for the Done
  bar's own height (DONE_BAR_HEIGHT) sitting on top of the keyboard.
- ✓ SHEET HEADER SCROLL-SHADOW BUG FIXED (round 181, resolves the old
  HIGH gap from round 152/153): root-caused via on-device debug data as a
  CSS stacking-order issue — a shadow cast by a normal-flow header can be
  silently painted over by a later sibling's own background unless the
  header has its own stacking context. Fixed with `relative z-10` on both
  the main Sheet's header and OverlayLayer's nav bar. Also fixed a related
  bug where OverlayLayer's persistent `scrolled` state leaked between
  different overlay screens, showing a stale shadow on a freshly-opened
  overlay before any scrolling.
- ✓ OVERLAY SHADOW BLEEDING ONTO GRAB HANDLE FIXED (round 181): the same
  shadow's 8px blur radius bleeds ~8px above its own box's top edge — in
  the main Sheet the grab handle sits inside that same shadow-casting box
  so the bleed lands above the panel's visible content and gets clipped;
  in OverlayLayer the grab handle was a separate, earlier sibling, so the
  bleed was visible squarely above the nav row once scrolled. Fixed by
  merging the grab handle and nav row into one shadow-casting box in
  OverlayLayer, matching the main Sheet's structure.
- ✓ GOAL FORK ENTRY ICON/TRANSITION FIXED (round 181): GoalScreen's
  "Set a goal" button now passes the same `from=today` param TodayScreen's
  two entry points already use, so GoalForkScreen ("What's your goal?")
  correctly shows an X (not a back arrow) and slides up from the bottom
  (not a plain cut) — GoalForkScreen is now device-reviewed and confirmed
  working (moved from LOW, "built but not device-reviewed"). An earlier
  attempt at this fix incorrectly targeted GoalSetupScreen's details step
  instead and was fully reverted before the correct fix landed.
- tsc: clean. lint: clean. vitest: 51 passed, 8 skipped (unchanged).

DONE (r180):
- ✓ ACCOUNT IA GROUPING PASS (round 180): resolved the long-standing
  "layout still growing" gap — Profile+BMR merged into one gauge-card
  layered component, Goal/Connections hidden, Data moved under a
  triple-tap Developer section, weight-units + weight-logging surfaced
  directly in Tracking. See CURRENT STATUS and CHANGELOG round 180 for
  full detail.

DONE (r170-179):
- ✓ GREY-FILL FULLY CONFIRMED: round 170's bottomGapPx cap on
  useFillToBottom (36px + 24px panel padding = ~60px max gap from true
  bottom) confirmed working on-device across all 3 grey-panel views, no
  further reports.
- ✓ 5 PANTRY BUGS (round 170): consistent 24px spacing; hide "Add
  another item" helper text + Save CTA in PantryMealDetail when nothing
  changed; X cancels (not saves) an in-progress name edit; removed
  redundant "Close" button in PantryFoodItemDetail; fixed naming-
  uniqueness + eager-save bug converting a Food item into a Meal via
  "add from pantry."
- ✓ SESSION-CHANGES REVERT-ON-X (round 170): X on an already-saved
  Meal now reverts ALL session changes (item add/remove, ingredient macro
  edits), not just the name field — only Save persists. Deliberately
  does not revert a measurementType/referenceAmount change (already
  triggered a one-way unit conversion elsewhere that reverting would
  leave silently wrong).
- ✓ APP-FREEZE BUG FIXED (round 171): X → discard-confirm →
  "Keep editing" no longer wedges the app. Root cause: Sheet.tsx's
  close() didn't respect closeImmediately for scrim-tap/swipe-dismiss,
  only the X button did; fixed + confirmed on-device via temporary debug
  logging.
- ✓ ADDANOTHERSECTION AUTO-SCROLL (round 172-173): expanding "+ Add a
  new food item" smoothly scrolls itself into view, with 16px of
  breathing room below.
- ✓ 12PX PHOTO TOP-SPACING: added (round 172), caused a shadow
  artifact on Meal-view photos, chased through 2 more rounds (173-174),
  then fully reverted (round 175) rather than keep patching around it —
  net no change from round 168's photo spacing.
- ✓ DARKER PHOTO SHADOW (round 176): new dedicated --shadow-photo
  token for real food/meal photos only, ~1.7x shadow-card-lg's opacity;
  shadow-card-lg itself untouched for its other (unrelated) card uses.
- ✓ 3-PILL PANTRY FILTER (round 177-178): Food items / Meals / My own
  independent toggle pills, replacing the old single-select All/Food
  items/Meals radio, in both PantryScreen and PantryPicker. New
  origin?: 'app' | 'user' field on FoodItem.
- ✓ 150 DEFAULT PANTRY FOODS SEEDED + MACROS LOCKED (round 179): see
  CURRENT STATUS above and CHANGELOG round 179 for full detail.

DONE (r168-169):
- ✓ MEAL-CARD LAYERED HIERARCHY: white meal-summary card + grey Food-items
  panel structure finalized across all 3 Basket Meal views (rounds 162-166);
  grey-fill root cause found and fixed round 168 (see HIGH — pending final
  on-device confirmation, not the design itself).
- ✓ DEFAULT MEAL/FOOD-ITEM IMAGE: real placeholder wired in round 166,
  swapped for Marco's smaller/preferred image round 168
  (src/assets/food-placeholder.png, 569KB); image radius bumped 20px→32px.
- ✓ AddAnotherSection border darkened (round 167: field→strong) and corner
  radius split 16px collapsed / 24px expanded (round 168).
- ✓ FoodForm's FAB-empty state (0 items) shows no image (round 167).
- ✓ FoodItemForm's "Add photo" CTA redesigned to match the real-photo box
  (round 167).
- ✓ Day's-log section bottom padding 12px→8px (round 168).
- ✓ Carb target model: replaced the leftover-calories residual formula
  with an activity-scaled body-weight-baseline model (round 169).

DONE (r122):
- ✓ FoodItemForm.tsx: unified shared form for all 4 food-item surfaces.
- ✓ PantryScreen: thin wrapper around FoodItemFormContent; delete icon updated.
- ✓ EditOverlay + ManualOverlay: replaced with FoodItemFormContent wrappers.
- ✓ "Save to pantry" from basket-edit: pantryItemId stamped on basket item;
    DB entry updated (foodItemId + isManual:false); photo forwarded correctly.
- ✓ addManualItem (both contexts): pantry id shared with basket item.
- ✓ "Saved to pantry" shows checked+disabled when item already linked.
- ✓ Photo cap 3 → 4 everywhere; ImageHero cfg3/cfg4 position tweaks.
- ✓ LogEntryContent frequentItems via useLive(frequentItemIds).
- ✓ Delete-item photo cleanup in LogEntryContent.
- ✓ addPantryItem photo wiring in LogEntryContent.
- ✓ Fast-log bypass when fromSearch=true (FoodPicker → FoodForm).
- ✓ Nutri-scan "Change" button removed from BasketCard.
- ✓ MeasurementTypeSelector in EditOverlay + ManualOverlay.
- ✓ PantryScreen delete icon updated to new filled design.

DONE (r121):
- ✓ FAB fully de-animated (hapticLight + onFabMorphComplete directly).
- ✓ BasketCard: always-visible edit+trash circular buttons (no swipe/editMode).
- ✓ EditIcon: custom SVG from design spec (16×16 fill path).
- ✓ All buttons + stepper container: h-10 (40px), icons 20px, stepper gap-0.
- ✓ ImageHero: 2/3/4-photo collage with Figma-corrected bounding-box coords.

DONE (r119):
- ✓ mealData.photos?: string[] added to FoodEntry type.
- ✓ FoodForm logBasket: saves photos[]; fallback 'Meal'→timeMealName().
- ✓ LogEntryContent: localPhotos init chain, editable meal name, onCorrect
    scoped to item.sourceId, correctingIdx state.

DONE (r115):
- ✓ LogEntrySheet: unified basket-style entry editor replacing EditFoodSheet
    + MealEditSheet. All 3 cases (pantry/manual/meal) shown as cards.
- ✓ BasketCard: "Fix/Change" button + onCorrect prop for re-describing scanned items.
- ✓ ManualOverlay: inline CTA Button, 256×256 photo style, keyboard scroll fix.
- ✓ Macro rounding: max 1 decimal, trailing zero trimmed (parseFloat toFixed(1)).

DONE (r114):
- ✓ Describe overlay redesign: 22-char name rules, non-food rejection, improved prompt.
- ✓ Vercel describe-food endpoint: model changed haiku → claude-sonnet-4-6.
- ✓ Sheet scroll: removed overflowX hidden from both scroll areas (iOS scroll chaining fix).
- ✓ OverlayNav centering: back button wrapped in w-10 shrink-0 span.
- ✓ AddAnotherSection: single-container pattern with bare FoodPicker prop.

DONE (r111):
- ✓ Overlay panels: swipe right → go back (touch handler on OverlayLayer).
- ✓ OverlayNav: shared sticky title bar + scroll shadow for all overlays.
- ✓ Single food photo: 256×256 square (was 4:3 wide).
- ✓ Duplicate pantry item in basket → increments qty instead of new entry.
- ✓ Meal header hidden when basket is empty.
- ✓ Frequent-foods quick-log grid removed from Diary.
- ✓ Log button always active (removed disabled state).
- ✓ Recent items "+" button: solid accent bg, black icon.
- ✓ Basket cards: removed thumbnail + protein, stepper always visible,
    tap card (normal mode) → edit overlay, edit mode → trash only beside
    card, swipe left reveals trash, returning from edit resets swipe state.
- ✓ Stepper at 0 → trash icon → tap removes item.
- ✓ "Add another item": grey bg, black text, visible in edit mode.
- ✓ Expanded picker: heading row with X left + title centred.
- ✓ Method cards: text-content (not secondary), overflow clipping fixed.
- ✓ ManualOverlay: photo upload added (same style as EditOverlay).
- ✓ EditOverlay: sticky nav via OverlayNav, non-sticky Save + Cancel,
    photo trash icon + "Change photo" white-border button.
- ✓ PantryScreen FoodItemForm: updated to new EditOverlay-style design.

DONE (r108):
- ✓ Add-Entry design spec written (leve-add-entry-design-spec.docx).
- ✓ Interactive HTML prototype built (add-entry-prototype.html).

DONE (r107):
- ✓ MacroDetailSheet horizontal clip without blocking vertical scroll.
- ✓ BreakdownSheet intro text now hides when info panel slides in.
- ✓ GoalSetupScreen Section 2 pre-fills from saved profile on every open.
- ✓ Gauge center number sign removed; sub-labels consistent.
- ✓ Custom filled info icon deployed everywhere.

DONE (r106):
- ✓ KEYBOARD/VIEW CONFLICT: content jumping + black void when keyboard opened
  in sheets. Fixed via @capacitor/keyboard resize:'none' + Capacitor
  keyboardWillShow/Hide events + scrollFocusedAboveKeyboard().

DEFERRED V1.5+: JSON import, BMR auto-calibration, full tablet
layouts, per-piece measurement types, time-of-day food suggestions.

----------------------------------------------------------------
RECENT ROUNDS (3 most recent, older entries in CHANGELOG.txt)
----------------------------------------------------------------

================================================================
ROUND 191: TESTFLIGHT DISTRIBUTION; APPLE HEALTH INTEGRATION; BRING-YOUR-OWN CLAUDE KEY + AI CONNECTION GATE; ACCOUNT SUB-VIEWS (2026-07-08)
================================================================
Files: capacitor.config.ts, ios/App/App/Assets.xcassets/AppIcon.appiconset/
       AppIcon-512@2x.png, ios/App/App/Info.plist, TESTFLIGHT.md (new),
       HEALTHKIT.md (new), src/data/healthkit.ts (new), src/domain/types.ts,
       src/domain/calc.ts, src/lib/apiKey.ts (new), src/lib/foodScan.ts,
       src/lib/scanErrors.ts (new), src/ui/AppShell.tsx,
       src/ui/components/AddEntrySheet.tsx, src/ui/components/ApiKeySheet.tsx
       (new), src/ui/components/HealthConnectBanner.tsx (new),
       src/ui/components/PantryFoodItemDetail.tsx,
       src/ui/components/PantryMealDetail.tsx,
       src/ui/components/PantryNewFoodScan.tsx, src/ui/components/Toaster.tsx,
       src/ui/components/WeightLogSheet.tsx, src/ui/components/useAiGate.tsx
       (new), src/ui/components/useFoodCapture.tsx, src/ui/kit/Icon.tsx,
       src/ui/kit/SlideScreen.tsx (new), src/ui/kit/index.ts,
       src/ui/screens/AccountScreen.tsx, src/ui/screens/GoalScreen.tsx,
       src/ui/screens/PastGoalsScreen.tsx, src/ui/screens/TodayScreen.tsx,
       package.json (added @capgo/capacitor-health,
       @aparajita/capacitor-secure-storage, @capacitor/app)

- BUG FIXES CARRIED OVER FROM ROUND 190: the Diary badge stuck on "No
  goal set" for the newly-shipped maintain_weight goal type (badge logic
  only checked lose/gain fields), and Past Goals' "Total days" showing a
  bogus 3650 for maintain goals (no targetDate to diff against, fell
  back to a 10-year sentinel). Both fixed in TodayScreen.tsx/
  PastGoalsScreen.tsx by branching on isMaintainGoal() the same way the
  rest of the app already does.
- ACCOUNT: SegmentedControl REPLACED WITH FilterPills (sex, weight units,
  weigh-in frequency in ProfileSheet/WeightUnitsCard/WeightCadenceCard),
  matches the pattern Goal Setup already used, for visual consistency
  across the app's two-and-three-option toggles.
- TESTFLIGHT DISTRIBUTION SET UP: fixed two blockers (app icon had an
  alpha channel, App Store Connect requires none, flattened to RGB;
  capacitor.config.ts's appId didn't match the Xcode project's real
  bundle ID, aligned to com.marcosilva86.nutritiongoal) and wrote
  TESTFLIGHT.md, a full step-by-step runbook (team/cert setup, App Store
  Connect app record, archive/upload, Export Compliance, Internal
  Testing). Confirmed Internal Testing needs no Beta App Review and the
  existing `npm run ios` / Xcode ▶ Run cable workflow is untouched and
  stays exactly as fast, TestFlight is a separate, occasional channel,
  not a replacement.
- APPLE HEALTH (HEALTHKIT) INTEGRATION SHIPPED, real, on-device, no
  backend, via @capgo/capacitor-health. Scope: weight (read into the same
  weight history as manual entries, tagged source:'healthkit', never
  overwrites a date already logged by hand) and activity/active-energy
  (additive: syncs at most one Activity entry per day, tagged
  source:'healthkit', refreshed in place as more of the day's data
  lands, coexisting with any manual entries that day rather than
  replacing them). Design went through several rounds of on-device
  feedback and converged on: (1) sync runs quietly on app open/foreground
  only, no continuous polling, plus on-demand from a synced day's own
  row; (2) no historical backfill, sync is clamped to never reach
  earlier than the moment Connect was actually tapped (connectedAt),
  so connecting after already using the app manually for days doesn't
  silently backfill weeks of data on top of existing entries; (3) a
  synced Activity row opens a read-only view (SyncedActivitySheet, not
  the normal edit form) with exactly two actions, an eye icon to
  Hide/un-hide the entry from every calorie total while it stays visible
  in Day's log with muted styling (ActivityEntry.hidden, a toggle, not a
  delete, a hidden day is also skipped by future syncs so it doesn't
  silently reappear), and a refresh icon to sync just that day on demand.
  Root-caused and fixed an early undercount bug (254kcal shown vs. 514
  actual): summing raw readSamples() client-side misses HealthKit's own
  overlapping-source deduplication (iPhone + Watch + a workout app each
  writing their own samples) and risked truncating before reaching
  today's most recent data at the sample-count limit, switched to
  Health.queryAggregated({bucket:'day', aggregation:'sum'}), which
  matches the Health app's own numbers. Account gained a connect/
  disconnect entry (now inside the Settings sub-view, see below) with a
  live status dot + description; contextual "Connect Apple Health"
  banners were added to Log Activity and Log Weight (HealthConnectBanner,
  dismissible nudge when not connected, persistent non-dismissible
  status line with relative last-sync time once connected, so connecting
  has a visible ongoing result instead of a nudge just vanishing). The
  one manual step: HealthKit's capability + entitlement has to be added
  via Xcode's Signing & Capabilities UI (Apple provisions this against
  the App ID, can't be done by hand-editing project files reliably);
  documented in HEALTHKIT.md along with the exact Xcode steps.
- BRING-YOUR-OWN CLAUDE API KEY (BYOK) FOR AI FOOD SCAN: Account >
  Settings > "AI Food Scan" opens a full-height sheet (ApiKeySheet) where
  a user pastes their own Anthropic API key, stored via
  @aparajita/capacitor-secure-storage (iOS Keychain / Android Keystore,
  not localStorage), with a masked (password-style) field with an eye
  toggle to reveal it, and numbered in-app instructions for getting a key
  from console.anthropic.com. When a personal key is set, lib/foodScan.ts
  calls api.anthropic.com directly from the device (model claude-sonnet-5,
  forced tool_use for structured ScannedFood[] output via Anthropic's
  anthropic-dangerous-direct-browser-access header) instead of going
  through the shared Vercel proxy, confirmed working end-to-end on a
  real device with a live key. No key set falls back to the existing
  shared proxy, unchanged for everyone who hasn't opted in.
- AI-FEATURE CONNECTION GATE (useAiGate): per Marco's explicit ask,
  replaced reactive "try the scan, show an error if it fails" handling
  with an upfront check. Every AI-feature trigger (Camera, Photo,
  Describe, Nutri-scan, and "Fix"/re-describe on an already-scanned item,
  across the Day's-log basket's two internal copies, the shared Pantry
  capture hook, and all three Pantry surfaces) now checks for a cached
  personal key immediately on tap, before the camera/picker/describe
  overlay ever opens. No key cached shows a short sheet ("Connect AI to
  use this", View more, which jumps straight to the ApiKeySheet above,
  or Cancel, which just closes) instead of proceeding. lib/apiKey.ts's
  in-memory key cache is warmed once on app boot so this check is
  synchronous. Reactive error handling (FoodScanError, toastScanError)
  was kept as a second layer for failures the gate can't predict, an
  invalid/rejected key, a network blip, or the shared proxy itself being
  down, showing a clear message plus a tappable action into the same
  key sheet, at every scan/describe/label-scan call site. This also
  fixed a real bug Marco caught while testing: removing a saved key used
  to silently keep working via the shared-proxy fallback; now the very
  next AI action after removing a key hits the gate instead.
- ACCOUNT RESTRUCTURED: Tracking and Settings are no longer shown inline
  on the main Account view, they're reached via a small "Options"
  section (two-row menu, Tracking/Settings) below Profile, each opening
  its own full-screen sub-view that slides in from the right with a back
  chevron (same treatment as Past goals), and now also responds to a
  swipe-right gesture to go back. Developer stays nested inside the
  Settings sub-view, not its own menu row, since it's a hidden
  triple-tap-title reveal, not a normal entry point. SlideScreen/
  SlideHeader were extracted out of PastGoalsScreen.tsx into
  src/ui/kit/SlideScreen.tsx (a proper shared kit atom, not duplicated
  per screen) and gained two new opt-in props used only by Tracking/
  Settings: `onBack` (enables the swipe gesture, same 60px/mostly-
  horizontal threshold Sheet's own OverlayLayer swipe uses) and `muted`
  (matches Account's own grey bg-surface-muted page backdrop instead of
  Past goals' white-sheet look), both additive, so Past goals' existing
  behavior and appearance are unchanged.
- TEXT/COPY POLISH: ApiKeySheet's "How to get a key" instructions bumped
  to 16pt (bold title, regular body) for readability. Swept every line
  of prose added across this entire round (comments and user-facing
  copy) for em dashes per Marco's standing preference, replacing them
  with plain punctuation, scoped precisely via diff against the
  session's first commit so the codebase's own pre-existing writing
  (which already used dashes as a long-standing house style) was left
  untouched.
- tsc: clean. lint: clean. vitest: 57 passed, 8 skipped (unchanged, no
  new automated tests this round; HealthKit/BYOK/gate logic was
  verified manually/on-device only, see KNOWN GAPS).

ROUND 190 — MAINTAIN-WEIGHT GOAL TYPE; GOAL-SETUP UI POLISH; KEYBOARD DONE-BAR ROOT CAUSE; NEW-FOOD/DESCRIBE FLOW FIXES; GOAL-FORK NAVIGATION (2026-07-07)
Files: src/domain/types.ts, src/domain/goal.ts, src/domain/goal.test.ts,
       src/ui/screens/GoalSetupScreen.tsx, src/ui/screens/FirstOpenFork.tsx,
       src/ui/screens/GoalIcon.tsx (new, extracted from GoalSetupScreen.tsx),
       src/ui/screens/GoalScreen.tsx, src/ui/screens/TodayScreen.tsx,
       src/ui/screens/PastGoalsScreen.tsx, src/ui/screens/AccountScreen.tsx,
       src/ui/screens/PantryScreen.tsx, src/ui/kit/WheelPicker.tsx,
       src/ui/kit/useKeyboardDoneBar.tsx, src/ui/components/AddEntrySheet.tsx,
       src/ui/components/PantryPicker.tsx, src/ui/components/PantryNewFoodScan.tsx,
       src/ui/components/FoodCapture.tsx, src/ui/components/DevMenu.tsx,
       src/lib/useKeyboardInset.ts, src/data/defaultPantrySeed.ts,
       src/data/defaultPantrySeed.test.ts, src/data/mealsMigration.ts,
       src/index.css

- MAINTAIN-WEIGHT GOAL TYPE SHIPPED: the third goal type (alongside
  lose_by_date/gain_by_date) is now fully implemented end-to-end.
  domain/types.ts gained weightRangeFloorKg/weightRangeCeilingKg,
  maintainBandId, and reviewDate on Goal. domain/goal.ts added
  isMaintainGoal/usesKcalBand/inBand helpers and MAINTAIN_BANDS presets
  (Tight +/-0.5kg/+/-100kcal, Standard +/-1kg/+/-150kcal, Relaxed
  +/-2kg/+/-250kcal); requiredDailyDeficit returns 0 for maintain.
  GoalSetupScreen: Guided mode picks a band preset via FilterPills with a
  live-computed weight-range + calorie-band summary card; Detailed mode
  adds a tappable pill opening a modal to choose Tight/Standard/Relaxed/
  Custom, with Custom revealing min/max wheel pickers. FirstOpenFork/
  GoalForkScreen gained a third "Maintain weight" path card. GoalScreen,
  TodayScreen, PastGoalsScreen all branch three ways now (previously
  hardcoded lose/gain only) -- band-based gauge/chart logic is shared
  between gain and maintain via the new usesKcalBand() helper. Fixed a
  pre-existing AccountScreen bug in the same pass: the goal-summary line
  hardcoded "lose Xkg" for every goal type, including gain goals.
- GOAL-SETUP UI POLISH (multiple rounds of on-device feedback): the
  Detailed mode's weight-range picker was redesigned from a free-text
  min/max pair to the "picker playground picker 3" single-wheel style
  (WheelPicker gained a `mode?: 'auto' | 'single'` prop forcing one flat
  wheel even for decimal steps, plus a float-precision fix in its snap()
  helper -- 71.3 no longer became 71.30000000000001 and broke the
  picker's selected-value match). That same single-wheel mode was later
  extended to the Start/Current/Target weight fields for Lose-weight and
  Build-muscle too (previously only Maintain used it), per follow-up
  feedback. Guided mode's single Weight+Range card and Detailed mode's
  single Goal-name+Weight+Dates card were each split into individual
  per-section cards (container styling unchanged, just separated).
  AccountScreen's weigh-in weekday pills were unified onto the shared
  FilterPills component (previously hand-rolled, visibly smaller than
  GoalSetupScreen's own).
- GOAL-FORK NAVIGATION TRANSITIONS FIXED (device-confirmed working):
  going back from Goal-setup to the goal-type fork screen (both the
  first-open onboarding fork and the regular in-app fork reached from
  Today) had two related bugs. (1) The fork screen only mounted AFTER
  Goal-setup's slide-out-right exit animation fully finished (route
  change happens on a delayed setTimeout), so the animation revealed a
  blank background for ~300ms before the destination screen popped in.
  (2) Returning to the fork screen when it was originally reached via
  Today (?from=today) replayed its whole slide-up "new modal" entrance
  animation every time, since a browser-back (POP) navigation to that
  same URL still carries the query param. Fixed by having GoalSetupScreen
  render a static, non-interactive peek of the correct destination screen
  (new FirstOpenForkBackdrop / GoalForkBackdrop components in
  FirstOpenFork.tsx) underneath itself while its own exit animation
  plays, so "back" reveals it directly instead of a gap; the entrance
  animation is now gated on useNavigationType() !== 'POP' so it only
  plays on a genuine fresh visit. GoalIcon was extracted out of
  GoalSetupScreen.tsx into its own file to avoid a circular import
  between the two screens.
- KEYBOARD "DONE" BAR GAP -- ROOT CAUSE FOUND AND FIXED (see KNOWN GAPS
  for confirmation status): the long-running "Done bar floats a few px
  above the keyboard" report (open since round 181) was chased across
  several attempts this round. A first hypothesis -- Capacitor's
  keyboardWillShow height excludes env(safe-area-inset-bottom) -- was
  implemented and made things dramatically WORSE (Marco: "completely out
  of place"), so it was reverted. Real root cause was then found using
  on-device diagnostics Marco captured via two new rows added to Dev >
  Keyboard playground (a live readout of keyboardInset/innerHeight/
  visualViewport.height/safe-area-inset-bottom, plus a 5th row using the
  actual production useKeyboardDoneBar hook on a plain text field): the
  Done bar is `position: fixed`, which normally positions relative to the
  true viewport -- EXCEPT when a transformed ancestor sits between it and
  the root, which becomes its containing block instead (any non-'none'
  CSS transform triggers this, even an identity translateX(0)). Sheet's
  own OverlayLayer (used by "Edit food item" and every other nested
  overlay) always has an active transform for its slide-in animation, so
  a Done bar rendered inside an overlay was positioning against THAT
  panel, not the screen -- matching Sheet.tsx's own existing comments
  about forceExpanded (91dvh) panels drifting slightly from the true
  viewport once the keyboard is open in this WKWebView setup (see its
  pre-existing "Surface cover" div, a patch for a different symptom of
  the same dvh quirk). Confirmed via A/B: a Done bar on a plain top-level
  Sheet field was pixel-perfect; the same hook nested inside an overlay
  showed the gap. Fix: useKeyboardDoneBar's bar now renders via
  createPortal straight to document.body (the same escape-hatch Sheet
  itself already uses for ancestor-constraint problems), so it always
  positions against the real viewport regardless of nesting depth.
- "+ NEW FOOD" / DESCRIBE FLOW FIXES: the Day's-log basket's "Change"
  button (re-run Describe on a scanned item) was wired for the initial
  scan-review step (previously only worked once editing an
  already-logged entry). Pantry > + New food > Describe now slides in as
  a genuine side-panel overlay (Sheet's useSheetSetOverlay/OverlayLayer)
  instead of swapping content in place, and expands the Sheet to full
  90dvh height while open (per follow-up feedback that it felt cramped)
  with the textarea autofocused so the keyboard is already up. Root-
  caused and fixed a bug where this overlay didn't open at all on first
  landing: useSheetSetOverlay/useSheetSetOverlayBack were being called in
  the same component that renders <Sheet>, one level ABOVE the context
  Sheet provides to its own children, so the registration was silently a
  no-op -- moved into a new PantryNewFoodBody child component, matching
  every other useSheetSetOverlay call site in the app. AnalyzingIndicator
  gained a `compact` prop (smaller padding) used only here, replacing an
  earlier attempt that instead padded the shorter states UP to match a
  tall loading screen (reverted per feedback: "too much negative
  screen"). Manual food creation now also lands on the Food-item detail
  preview in "just created" mode (previously only scanned items did), so
  it gets the same "Save food" CTA and discard-on-close confirmation.
- SCROLLBAR CHROME HIDDEN APP-WIDE (device-confirmed): on-device, the
  visible WKWebView scrollbar track was poking through the native
  keyboard's own rounded top corners as a stray grey sliver while
  scrolling with the keyboard open. Added a global
  scrollbar-width:none / ::-webkit-scrollbar{display:none} rule
  (src/index.css) -- content scrolls identically, just without visible
  scrollbar chrome, matching how native iOS apps look.
- PANTRY SEED / MEALS MIGRATION NOW PER-PROFILE: both one-time seed flags
  (default pantry items, meals migration) were stored under a single
  global localStorage key shared across the app's real/test dual-profile
  system, so seeding the real account permanently skipped seeding the
  test account (and vice versa). Both flags are now scoped per active
  profile (defaultPantrySeedFlagKey/mealsMigrationFlagKey); DevMenu's
  test-account reset paths clear them too so wiping the test DB reseeds
  it. Added a regression test asserting the flag key differs per profile.
- tsc: clean. lint: clean. vitest: 57 passed (6 new), 8 skipped.
  `npm run build` verified clean after every sub-batch this round.


ROUND 181 — KEYBOARD/UX POLISH BATCH: DONE BAR, CARB CAP, GOAL ICONS, INFO CARD, STATUS-BAR FADE, SHEET SHADOW FIXES, GOAL FORK ENTRY (2026-07-06)
Files: src/ui/kit/useKeyboardDoneBar.tsx (renamed from useNumericDoneBar.tsx),
       src/ui/kit/LabeledInput.tsx, src/ui/kit/NumberField.tsx,
       src/ui/kit/Field.tsx, src/ui/screens/PantryScreen.tsx,
       src/domain/goal.ts, src/ui/kit/Icon.tsx, src/ui/screens/GoalScreen.tsx,
       src/ui/screens/GoalSetupScreen.tsx, src/ui/AppShell.tsx,
       src/ui/kit/Sheet.tsx, src/lib/useKeyboardInset.ts
- KEYBOARD DONE BAR UNIFIED: useNumericDoneBar renamed useKeyboardDoneBar
  and moved into LabeledInput so every text field (not just numeric ones)
  gets the same "Done" dismiss accessory bar as the numeric keyboard.
  NumberField simplified to just render LabeledInput. An early attempt
  forced the bar to a fixed 44px height on the assumption numeric/text
  variants rendered differently — Marco reported it "got even bigger";
  re-investigation confirmed both cases always shared the exact same
  component, so the forced height was reverted back to natural py-2
  sizing (DONE_BAR_HEIGHT corrected 44 -> ~40, see KNOWN GAPS re: this
  being a CSS-math estimate, not device-measured).
- BALANCED CARB TARGET CAP: activityCarbTargetG() (src/domain/goal.ts)
  now applies a 4.5g/kg hard cap for the Balanced macro style only
  (CARB_MODEL.balanced.capPerKg); Performance stays uncapped. Fixes the
  ~400g/day carb target outlier Marco saw on high-activity days under
  Balanced. Cap value itself is a first estimate, not yet validated.
- GOAL-OPTIONS AFFORDANCE: the "⋯" (moreHoriz) options icon in
  GoalScreen's two triggers was first swapped for a custom Marco-supplied
  gear icon (settingsGear, added to Icon.tsx), then replaced again per
  follow-up feedback with a plain "Options" text label
  (<Button variant="ghost" ...>Options</Button>). settingsGear and its
  Icon.tsx wiring were fully removed again afterward; moreHoriz itself
  is still defined but now unused anywhere in the app (see KNOWN GAPS).
- 44×44PT CLEAR-BUTTON HIT AREAS: audited and fixed all three clear (X)
  button implementations to meet the app's own touch-target rule.
  LabeledInput's absolutely-positioned button resized directly to
  h-11 w-11 (44×44) with the icon centered. Field's and PantryScreen's
  search-bar in-flow buttons use a -m-[14px]/p-[14px] padding +
  negative-margin trick that expands the tap target without growing
  the visible layout; PantryScreen's button also gained a missing
  type="button" and onMouseDown preventDefault.
- GUIDED/DETAILED GOAL-SETUP RELABEL + MERGED INFO CARD: the Create/edit
  goal flow's Simple/Custom segmented control is now labeled
  Guided/Detailed, with mode-specific copy ("We'll estimate your calories
  and macros..." / "Choose your own calorie and macro targets..."). Added
  an outline info card visually merged behind the segmented control
  (its top border crossing behind the pill's vertical center), reviving
  the round-65 design pattern. Follow-up fix: the segmented control was
  living in the sheet's sticky header, whose opaque background hid the
  info card's top border wherever it poked into that row — fixed by
  moving the segmented control out of the sticky header into normal
  scrolling flow (only the title now stays sticky). This also resolves
  the long-open "SHEET stickyHeader SLOT" HIGH gap from round 152/153,
  since that slot is no longer used for this control at all.
- TOP STATUS-BAR FADE GRADIENT: added a white gradient fade over the iOS
  notch/status-bar area app-wide (AppShell.tsx), mirroring the existing
  bottom-nav fade — pointer-events-none, sits on top, never affects
  content position; content scrolls through it unaffected, matching the
  "graceful — notch + extra runway" option Marco chose. Also experimented
  with an onTouchStart handler on the existing tap-to-scroll-top
  interceptor to fix a dead zone when tapping exactly on the clock;
  Marco reported it "didn't really work" but it's harmless and was left
  in place (see KNOWN GAPS — likely a WKWebView hit-testing limitation).
- KEYBOARD-COVERS-CONTENT BUG FIXED: several sheets were hiding form
  content behind the keyboard again. Root cause: OverlayLayer (used by
  "Edit food item" and other in-sheet overlays) was missing scroll-into-
  view logic entirely, unlike the main Sheet. Added the same Case-1/
  Case-2 focusin scroll-into-view effects to OverlayLayer, and corrected
  the keyboard-clearance math everywhere (Sheet.tsx, OverlayLayer,
  GoalSetupScreen.tsx, useKeyboardInset.ts's scrollFocusedAboveKeyboard)
  to add DONE_BAR_HEIGHT on top of the raw keyboard inset, since the
  Done bar sits on top of the keyboard rather than inside its reported
  height.
- SHEET HEADER SCROLL-SHADOW BUG FIXED (careful mode; resolves the open
  HIGH gap from round 152/153): Marco reported the "shadow casts when
  content scrolls under the sheet header" effect had regressed. Initial
  hypothesis (shadow too subtle to notice) was explicitly and firmly
  rejected by Marco ("there's a different issue at play, I'm 100% sure").
  Root-caused via an on-device debug readout Marco captured (temporarily
  added, then removed) showing scrolled=true even when no shadow was
  visible: a CSS stacking-order issue where a shadow cast by a normal-
  flow header can be silently painted over by a later sibling's own
  background unless the header has its own stacking context. Fixed with
  relative z-10 on both the main Sheet's header and OverlayLayer's nav
  bar. Cross-checked against the round-152/153 CHANGELOG history (a
  prior fix attempt caused the panel's rounded corners to go square) and
  confirmed this fix couldn't reproduce that regression, since it adds
  no new opaque background — only a stacking-context change.
  Also fixed a related bug surfaced by the same fix: OverlayLayer's
  scrolled state persists across different overlay screens (only the
  rendered content swaps), so a freshly-opened overlay (e.g. "Edit food
  item") could inherit scrolled: true from whatever was scrolled before
  it and show a stale shadow before any scrolling. Reset scrolled to
  false whenever OverlayLayer's node prop changes.
- OVERLAY SHADOW BLEEDING ONTO GRAB HANDLE FIXED: after the above fix,
  Marco spotted a new, related artifact — a faint shadow visible above
  OverlayLayer's nav row, over the grab handle, once scrolled. Root
  cause: shadow-nav's box-shadow (0 2px 8px -2px, src/index.css) has an
  8px blur radius that bleeds roughly that far above its own box's top
  edge. In the main Sheet, the grab handle lives inside the same
  shadow-casting box, so that bleed lands above the panel's visible
  content and gets clipped by the panel's own overflow-hidden/rounded
  top corner. In OverlayLayer the grab handle was a separate, earlier
  sibling sitting below that clipped zone, so the bleed was visible
  squarely between the grab handle and the nav row. Fixed by merging
  the grab handle and nav row into one shadow-casting box in
  OverlayLayer (same padding/margin math preserved), matching the main
  Sheet's structure so the bleed gets clipped the same way.
- GOAL FORK ENTRY ICON/TRANSITION FIXED: Marco asked for the "start a
  new goal" screen to show an X instead of a back arrow, and to slide up
  from the bottom instead of its current transition. First attempt
  incorrectly targeted GoalSetupScreen's details step (the wrong
  screen) and was fully reverted after Marco pointed out the actual
  screen was GoalForkScreen ("What's your goal?", /goal-fork). That
  screen already has the exact X-icon + slide-up-in treatment, gated
  behind a from=today query param already used by TodayScreen's two
  goal-fork entry points — GoalScreen's "Set a goal" button was calling
  nav('/goal-fork') with no param, landing in the back-arrow/no-
  animation branch instead. Fixed by reusing the existing from=today
  param on GoalScreen's button; no new code paths needed. GoalForkScreen
  is now device-reviewed and confirmed working (moved from a LOW "built
  but not device-reviewed" gap to DONE).
- tsc: clean. lint: clean. vitest: 51 passed, 8 skipped (unchanged).

================================================================
TODO / FUTURE WORK
================================================================
- "+NEW FOOD" LOADING + TRANSITION POLISH: round 150 fixed this for the
  Camera/Photo/Describe/Nutri-scan methods — they now live inside ONE
  persistent Sheet with phase-based content (picker -> spinner/Describe ->
  commit), no more sheet-swap flash. NOT yet applied to Manual: tapping
  Manual still hands off to a separate forceExpanded form Sheet, and the
  New-food-form -> Food-item-detail transition there still slides one sheet
  down then a new one up rather than happening "in place" — Marco's
  original ask was for a dedicated ~2s "Saving food" loading screen there
  too, still outstanding.
- NUTRI-SCAN vs. CAMERA/PHOTO/DESCRIBE consistency: raised in round 150
  (Nutri-scan also reads whole food, not just labels, making its
  always-ask-per-100g/serving behaviour feel inconsistent with the other 3
  methods) — Marco decided (round 151) to leave this alone for now.
- BASKET-VIEW UNIFICATION (partially done, round 124): DeleteIcon/EditIcon and
  AddAnotherSection/MethodCards are now genuinely shared (icons.tsx,
  MethodCards.tsx) between the Day's-log basket and every Pantry surface —
  one change to either reflects everywhere. Still intentionally NOT unified:
  PantryItemCard vs BasketCard themselves stay separate components (opposite
  stepper rules — Pantry disabled vs Day's-log enabled — and different trash
  semantics). Worth a dedicated round to decide how far a full "one card,
  many contexts" unification should go, rather than doing it as a side-effect
  of an unrelated change.
- KEYBOARD TOOLBAR (accessory bar): round 160 closed the "no closing
  action" half of this gap — every numeric-keyboard input now shows a
  "Done" bar (useNumericDoneBar). Still open: prev/next field navigation
  between inputs inside a multi-field sheet (GoalSetupScreen Section 2,
  ProfileSetupSheet, EditFoodSheet) — Capacitor InputAccessoryView or a
  custom UIToolbar injected via WKWebView bridge would be needed for that.
