================================================================
NUTRITION GOAL TRACKER — PROJECT CONTEXT
================================================================
Owner: Marco (marcosilva86@gmail.com)
Last updated: 2026-06-28 — Round 108 (Add-Entry flow design sprint: spec doc + interactive prototype)
Full change history: see CHANGELOG.txt (rounds 1–106).

HOW TO READ THIS DOC:
- This file = current state only. Read it at the start of every chat.
- CHANGELOG.txt = full per-round history. Open it when you need to know
  WHY something was decided or what changed in a specific round.
- Before touching any UI: read section 0B (Design-System Contract) first.
- If two statements conflict: the higher round number wins.

----------------------------------------------------------------
0. QUICK STATUS
----------------------------------------------------------------
- App is RUNNING on Marco's iPhone (native Capacitor build, free Apple ID).
  On-device install: DONE as of 2026-06-04 (runbook: INSTALL-iOS.md).
- THREE PREVIEW SURFACES, each with its OWN separate data store:
  (1) iPhone native app (Capacitor) — Marco's REAL daily-use data; IndexedDB
      in the app container; persists across rebuilds/re-signing.
  (2) Browser dev server (`npm run dev` -> localhost) — real app with
      hot-reload; IndexedDB local to that browser; SEPARATE from the phone.
  (3) Double-click demo HTML (file://) — in-memory, resets on refresh.
  Data does NOT sync between surfaces.
- V1 FEATURE STATUS: feature-complete and running on phone.
- VERIFY-ON-MAC REMINDER: after Cowork changes, run on Mac:
  `npm install` → `npm test` → `npm run build` → `npm run ios:sync`.

----------------------------------------------------------------
0B. DESIGN-SYSTEM CONTRACT — FIXED RULES (read BEFORE editing any UI)
----------------------------------------------------------------
Non-negotiable standing rules. Any chat touching src/ui MUST follow them.

1. COMPONENTS FIRST. Build UI from kit atoms in src/ui/kit: Button/IconButton,
   Sheet, LabeledInput, NumberField, Card/Surface, StatTile, ProgressBar/
   GaugeArc, Badge, SegmentedControl, FilterPills, Icon, etc. NEVER hand-roll
   a button/input/sheet/stat inline when a kit atom exists.
2. TOKENS ONLY. Colour, radius, shadow and type from src/index.css semantic
   tokens. NEVER raw Tailwind text sizes (text-xs/sm/base/lg/xl) — use
   text-caption/footnote/subhead/body/callout/headline/title/display/hero.
   NEVER hardcode hex/rgb — use colour tokens.
3. ICONS, NOT GLYPHS. Use <Icon> for all affordances. Never type ›, ‹ or ×.
4. ONE SOURCE OF TRUTH. Change look via token or atom, never per-screen CSS.
5. ENFORCEMENT. eslint.config.js BLOCKS raw text sizes and hex/rgb in src/ui.
   Run `npm run lint` before shipping. Do NOT disable guardrails.
6. ACCESSIBILITY FIRST. Focus outlines, ARIA roles, prefersReducedMotion(),
   WCAG AA contrast, 44×44pt touch targets, labeled form inputs.

Text scale: caption(12) / footnote(13) / subhead(15) / callout(16) /
headline(18) / title(22) / display(30) / hero(48).
WHERE THINGS LIVE: tokens = src/index.css (@theme); atoms = src/ui/kit/;
live reference = /styleguide route.

----------------------------------------------------------------
1. WHAT WE'RE BUILDING
----------------------------------------------------------------
A personal nutrition + weight-management app for Marco's own use.
NOT a calorie tracker. Core idea: GOAL MANAGEMENT — tracking exists only
to answer "Am I on track to hit my goal?"
Mental model: Goal → Weekly progress → Daily progress → Food/Activity inputs.

----------------------------------------------------------------
2. PLATFORM & STACK
----------------------------------------------------------------
React + Capacitor (web code + native iOS shell). NOT native Swift.
Stack: Vite 6, React 19, TypeScript, Tailwind CSS v4, Dexie (IndexedDB),
Vitest. Capacitor v8. appId: com.marco.nutritiongoal, webDir: dist.
Install path: Xcode + free Apple ID ($0). Re-sign every ~7 days.

----------------------------------------------------------------
3. AGREED V1 SCOPE
----------------------------------------------------------------
- Goal types: lose_by_date and gain_by_date.
- TODAY: frequent foods, Add-entry sheet, calories + protein, deficit,
  swipeable day nav.
- GOAL: weight trend vs. target line, 9 lifecycle states.
- PANTRY: create/edit food items.
- Manual meal entry, manual activity calories, weight logging.
- BMR: single user-entered number in V1.
- Account: weight history, profile, BMR, export (JSON).
DEFER (V1.5+): BMR auto-calibration, HealthKit/Withings, History tab,
per-piece measurement types.

----------------------------------------------------------------
4. NAVIGATION / IA
----------------------------------------------------------------
- Bottom tab bar: Today / Goal / Pantry / Account.
- Center "+" FAB speed dial: Scan / Food / Activity / Weight.
  Scan item only shown when VITE_FOOD_SCAN_API_URL is set (SCAN_ENABLED gate).
- Add-entry = bottom sheet (never full-screen nav).
- Goal Setup = full-screen flow. ?skip-type=true skips plan-type step.
- Day nav on Today: chevrons + swipe. Future dates disabled.

----------------------------------------------------------------
4B. V1 DATA MODEL
----------------------------------------------------------------
Goal status: active / completed / abandoned.
GoalScreen renders 9 states (all implemented r55):
  (1) no-goal  (2) active-normal  (3) final-day·no-weight
  (4) final-day·weight-in(not hit)  (5) final-day·goal-hit
  (6) early-complete  (7) overdue  (8) completed  (9) abandoned/ended
GoalOutcomeView: createPortal → document.body. Slide-up on mount.
GoalSettingsSheet (⋯): Mark complete / Edit plan / End goal / Past goals.
User model: weightCadence ('daily'|'weekly'), weeklyWeightDay.

----------------------------------------------------------------
5. METRICS / MATH
----------------------------------------------------------------
Total Burn = BMR + Active Calories.
Daily Deficit = Total Burn − Calories Consumed.
~7700 kcal per kg for goal pace math.

----------------------------------------------------------------
7. CURRENT STATUS (as of round 108, 2026-06-28)
----------------------------------------------------------------
All V1 features shipped and running on device. Recent work:
- Rounds 85–107: All V1 UI work complete (see CHANGELOG.txt).
- Round 108 (2026-06-28): Design sprint — Add-Entry flow redesign.
  No source code changes. Deliverables:
  · leve-add-entry-design-spec.docx — 15-section design spec, 733
    paragraphs, 5 embedded Figma screenshots, codebase type mapping,
    kit component inventory, motion specs, z-index stack.
  · add-entry-prototype.html — interactive wireframe-quality HTML
    prototype covering all states (basket, overlays, edit mode, etc).
  Both files live in Documents/leve/ root.
  NEXT: Implement the Add-Entry redesign (basket-first UX) based on the
  spec. Main file: src/ui/components/AddEntrySheet.tsx (~1200 lines).
- .env.local: gitignored, must be recreated if folder is deleted.
  VITE_FOOD_SCAN_API_URL=https://food-scan-api-delta.vercel.app
- tsc: clean. lint: clean (r107 — no code changes in r108).

----------------------------------------------------------------
8. ARCHITECTURE NOTES
----------------------------------------------------------------
Layer model (strict dependency direction):
  src/domain/   Pure TS — types.ts, calc.ts, goal.ts, bmr.ts.
  src/data/     Dexie + memory repos. repositories.ts = interfaces.
  src/state/    repos.ts (swap point) + useLive() + useDay().
  src/ui/       AppShell + screens + components + kit/.
  src/index.css Design token layer (Tailwind v4 @theme).

KEY PATTERNS:
- createPortal: GoalOutcomeView renders into document.body to escape
  AppShell overflow-hidden / z-index on WKWebView.
- slide-up/down: .slide-up-in / .slide-down-out in index.css.
  Exit: setExiting(true) → setTimeout(cb, 320) → navigate.
- Sheet-on-Sheet: destructive confirms render a second Sheet alongside
  the parent in a Fragment (both conditionally mounted).
- SCAN_ENABLED = !!(import.meta.env.VITE_FOOD_SCAN_API_URL) — used in
  AddEntrySheet.tsx header icon and FloatingTabBar.tsx FAB action list.

ENV NOTE: Cowork Linux sandbox can't run rollup native binary. After
changes, on the Mac: `npm install` → `npm test` → `npm run build` →
`npm run ios:sync`.

----------------------------------------------------------------
KNOWN GAPS
----------------------------------------------------------------
HIGH (next round):
- ADD-ENTRY FLOW REDESIGN: basket-first UX not yet implemented.
  Spec: leve-add-entry-design-spec.docx (Documents/leve/).
  Prototype: add-entry-prototype.html (Documents/leve/).
  Main file to restructure: src/ui/components/AddEntrySheet.tsx (~1200 lines).
  Key changes: multi-item basket, image hero/collage, meal name, save-to-pantry,
  method cards (Camera/Photo/Describe/Label), edit-mode overlay, analysis overlay.

MED:
- NO CONFIRM on destructive deletes of food entries, pantry items, weight
  entries. (Goal actions confirm via Sheet; entry deletes are immediate.)
- ACCOUNT IA: layout still growing — grouping pass worthwhile when more
  prefs land.
- .env.local is gitignored and must be recreated manually if the project
  folder is deleted. Contains VITE_FOOD_SCAN_API_URL (Vercel scan API URL).

LOW:
- GoalOutcomeView: no haptic on appearance.
- App.tsx still has /past-goals and /past-goals/:id stub routes (now unused;
  PastGoalsPortal is portal-based). Safe to remove eventually.
- ACTIVITY TEACHING MOMENT (§9 first-open spec): deferred. Component ready;
  copy for gain/explore paths not finalised.
- GoalForkScreen (/goal-fork): built but not device-reviewed.
- ProfileSetupSheet height select always shows cm. Users set to lbs see no
  ft/in option. Low priority until imperial height preference is added.
- Estimated BMR (weight-only) assumes pop-avg 170 cm / 35 yr / neutral sex.
  Very tall/short or young/old users may see a notably off estimate until
  they complete their profile.

DONE (r108):
- ✓ Add-Entry design spec written (leve-add-entry-design-spec.docx).
- ✓ Interactive HTML prototype built (add-entry-prototype.html).
- ✓ Edit mode now hides add-another tray entirely (prototype + spec updated).
- ✓ Save-to-pantry confirmed unchecked by default (prototype + spec updated).
- ✓ Meal header confirmed always visible; Edit button hidden when basket empty.

DONE (r107):
- ✓ MacroDetailSheet horizontal clip without blocking vertical scroll.
- ✓ BreakdownSheet intro text now hides when info panel slides in.
- ✓ GoalSetupScreen Section 2 pre-fills from saved profile on every open.
- ✓ Gauge center number sign removed; sub-labels consistent (food +, activity −).
- ✓ Custom filled info icon deployed everywhere; color matches adjacent text.

DONE (r106):
- ✓ KEYBOARD/VIEW CONFLICT: content jumping + black void when keyboard opened
  in sheets. Fixed via @capacitor/keyboard resize:'none' + Capacitor
  keyboardWillShow/Hide events + scrollFocusedAboveKeyboard() +
  touchAction:pan-y on sheet outer container.

DEFERRED V1.5+: JSON import, HealthKit, BMR auto-calibration, full tablet
layouts, per-piece measurement types, time-of-day food suggestions.

----------------------------------------------------------------
RECENT ROUNDS (3 most recent — older entries in CHANGELOG.txt)
----------------------------------------------------------------

ROUND 108 — ADD-ENTRY FLOW DESIGN SPRINT: SPEC DOC + INTERACTIVE PROTOTYPE (2026-06-28)
Files: leve-add-entry-design-spec.docx (new), add-entry-prototype.html (new),
       leve/PNGs/ (5 Figma frame exports)
No source code changes — design sprint only.
- Created add-entry-prototype.html: interactive wireframe-quality behavioural
  prototype covering all states of the Add-Entry sheet (basket, all 5 input
  method overlays, edit mode, analysis overlay, serving modal, etc.).
  Built in vanilla JS/CSS; runnable standalone from file:// with no server.
- Created leve-add-entry-design-spec.docx (v1.1, 733 paragraphs, 5.4 MB):
  15-section design spec covering full Add-Entry flow redesign.
  Key sections:
  · Overview & Principles, Design Tokens, Add Sheet (motion + z-index stack)
  · Food Pane with 5 embedded Figma screenshots (empty/1-item/edit/2-items/3-items)
  · 5 Input Methods (Camera, Photo, Describe, Label, Manual)
  · Image Hero/Collage, Edit Overlay, Data Model, Analysis Overlay
  · Activity + Weight panes, Day's Log Edit Meal
  · Component Inventory (maps to actual AddEntrySheet.tsx)
  · Section 15: Codebase Reference — spec-to-type mapping, kit components,
    supporting infra (camera.ts, foodScan.ts, haptics.ts, etc.)
  · Appendix: 5 Figma frame hyperlinks + prototype reference
- UX decisions confirmed:
  · Edit mode hides add-another tray entirely (spec + prototype updated)
  · Save-to-pantry unchecked by default (prototype checkbox bug fixed)
  · Meal header always visible; Edit button hidden when basket is empty
  · Meal name input only shown at 2+ items in basket
- tsc: clean. lint: clean (no code changes).

ROUND 107 — UX POLISH: MACRO SHEET CLIP, BREAKDOWN INTRO, GOAL PRE-FILL, GAUGE + INFO ICON OVERHAUL (2026-06-26)
Files: src/ui/screens/TodayScreen.tsx, src/ui/screens/GoalSetupScreen.tsx,
       src/ui/kit/Icon.tsx
- MacroDetailSheet: container changed from overflow-hidden to overflowX:'clip'
  (inline style). Clips horizontal slide animation without creating a block
  formatting context that cuts off vertically-overflowing content.
- BreakdownSheet: "Food adds energy…" intro paragraph was a direct Sheet child
  (sibling of scrollable area), so it stayed fixed during info panel slide-in.
  Moved inside the first sliding div — now hides with the main panel.
- GoalSetupForm: added direct useLive(() => repos.user.get(), []) query. Section 2
  (height/age/sex) pre-fills from saved profile on every open. Functional
  updater `prev !== null ? prev : value` prevents overwriting user edits.
  ESLint react-hooks/set-state-in-effect suppressed on first setState only.
- Gauge center number: removed +/− prefix. Now shows plain absolute number
  matching the style used in the calorie breakdown.
- Gauge sub-labels: Food = plain positive number, Activity = always −N. Removed
  stray + that was appearing on Food.
- MacroDetailRow: reverted icon order → label text, then small info icon (was
  icon-first after a prior round accidentally flipped it).
- Daily target container (all 3 panels): added text-center; label and value
  both changed to text-content (was text-content-secondary).
- Gauge badge: info Icon (size 12) added as child of Badge component, after
  label text, to signal tappability → opens MacroDetailSheet.
- Icon.tsx: custom filled info icon replaces stroke version everywhere.
  · Path sourced from Icon_info.svg (20×20 viewBox, filled circles).
  · 'info' added to FILL_ICONS set and CUSTOM_VIEWBOX map (viewBox '0 0 20 20').
- Info icon color: instances paired inline with secondary-text labels changed
  from text-content-muted → text-content-secondary.
  · MacroDetailRow shrink-0 icon.
  · All three breakdown math-row icons (replace_all).
  · Standalone decorative icons (BMR card, "Numbers estimated" banner) left
    as text-content-muted.
- tsc: clean. lint: clean.

ROUND 106 — AUDIT FIXES + PICKER→INPUT MIGRATION + KEYBOARD OVERLAY + SHEET POLISH (2026-06-25)
Files: src/lib/useKeyboardInset.ts (new), src/ui/kit/Sheet.tsx,
       src/ui/screens/GoalSetupScreen.tsx, src/ui/components/AddEntrySheet.tsx,
       src/ui/screens/AccountScreen.tsx, src/ui/screens/GoalScreen.tsx,
       src/domain/pantrySeed.ts, src/main.tsx, package.json, capacitor.config.ts
- Audit fixes:
  · AccountScreen: bg-neutral-300 → bg-border-strong (token violation).
  · pantrySeed.ts: removed direct state/repos import; now accepts FoodItemRepo
    as parameter. main.tsx passes repos.foodItems at call site.
  · GoalScreen stat tiles: added aria-label to 3 icon-only button tiles.
- NumberField rewritten: WheelPicker removed; wraps LabeledInput with
  type="number" inputMode="decimal". All macro/nutrition/servings/quantity(g)
  fields use plain text input. ServingStepper +/− kept.
  Activity calories WheelPicker → LabeledInput in AddEntrySheet + TodayScreen.
- Keyboard overlay (KeyboardResize.None):
  · package.json: @capacitor/keyboard ^8.0.0.
  · capacitor.config.ts: Keyboard plugin { resize: 'none', style: 'default' }.
  · src/lib/useKeyboardInset.ts: Capacitor keyboardWillShow/Hide events (native);
    visualViewport fallback (browser). scrollFocusedAboveKeyboard() uses
    getBoundingClientRect() vs keyboard top (not scrollIntoView).
  · Sheet.tsx: useKeyboardInset(); stable focusin listener; touchAction:pan-y
    (was 'none' — was suppressing manual scroll); paddingBottom = keyboardInset.
  · GoalSetupScreen.tsx: same keyboard padding + scroll-into-view pattern.
- Sheet polish:
  · Header shadow-nav on scroll (scrolled state + onScroll on scroll area).
  · Activity Estimate tab: labels text-subhead, selects bg-surface-sunken.
  · Rounded-t-sheet corners restored: removed bg-surface from header wrapper.
- tsc: clean. lint: clean.

================================================================
TODO / FUTURE WORK
================================================================
- KEYBOARD TOOLBAR (accessory bar): Add a prev/next field navigation bar +
  dismiss button above the keyboard when number/text inputs are focused inside
  sheets. Capacitor InputAccessoryView plugin or a custom UIToolbar injected
  via WKWebView bridge. Improves UX for multi-field forms (GoalSetupScreen
  Section 2, ProfileSetupSheet, EditFoodSheet).

================================================================

ROUND 104 — SIMPLE MODE OUTLINED PICKERS + SCAN FAB GATE (2026-06-24)
Files: src/ui/screens/GoalSetupScreen.tsx, src/ui/kit/FloatingTabBar.tsx
- Simple mode WheelPickers: removed sunken selectClassName → outlined fields.
  Custom mode retains filled (inside white card).
- FloatingTabBar: SCAN_ENABLED gates "Scan meal" ACTION_ITEM. Prevents
  "Food scan not configured" error when VITE_FOOD_SCAN_API_URL is unset.
- tsc: clean. build: clean.
